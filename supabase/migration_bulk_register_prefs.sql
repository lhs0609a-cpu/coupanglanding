-- ============================================================
-- 대량 상품 등록: 사용자별 영구 설정 저장
-- ============================================================
-- 목적: 배송/반품 설정, 마진율, 옵션 등을 계정 단위로 저장
--       (브라우저 localStorage 의존 제거 → 다른 기기/브라우저에서도 유지)
-- ============================================================

ALTER TABLE megaload_users
  ADD COLUMN IF NOT EXISTS bulk_register_prefs JSONB;

COMMENT ON COLUMN megaload_users.bulk_register_prefs IS
  '대량 상품 등록 1단계 설정값 (출고지/반품지/배송비/반품비/연락처/마진율/옵션 등)';
