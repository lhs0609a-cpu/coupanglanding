# 네이버 소싱 카탈로그 — 카테고리 트리 설계도

작성 2026-08-20 · 대상 화면 `/megaload/sourcing/naver` (`src/app/megaload/sourcing/naver/page.tsx`)

## 0. 한 줄

셀러가 카탈로그에서 **카테고리를 골라** 그 안의 상품만 보고, 고른 것을 그대로 올인원 등록까지
끌고 갈 수 있게 한다. 지금은 상품명 검색밖에 없어서 "무슨 물건이 쌓여 있는지" 를 볼 방법이 없다.

---

## 1. 지금 상태 (실측 2026-08-20, 프로덕션 DB)

| 항목 | 실측 |
|---|---|
| 카탈로그 행 수 | **59** |
| `naver_category_id` 채워진 행 | 54 / 59 (distinct **18**) |
| `category_path` 값 | `신선식품 > 과일` **55건**, `식품>농산물>과일>토마토` 등 **4건** |
| `detail_status` | none 53 · done **4** · failed 2 |
| 도우미 트리 스냅샷 | ~~401 노드 (대분류 25 + 중분류 376), **depth 2**~~ |
| → 2026-08-20 갱신 | **3,166 노드** (대분류 27 + 중분류 377 + 소분류 2,762), **depth 3** |

읽어야 할 사실 세 가지.

1. **화면엔 카테고리 입구가 없지만 API 는 이미 받는다.** `GET /api/megaload/naver-sourcing/products`
   는 `?category=` 를 받아 `naver_category_id` 로 `eq` 필터를 건다. 서버는 준비돼 있고 화면이 없다.
2. **`category_path` 는 두 가지 형식이 섞여 있다.** 55건은 수집 때 관리자가 고른 이름(`catName`,
   공백 있는 ` > `), 4건은 상세 추출이 덮어쓴 네이버 표준 분류(공백 없는 `>`). 이 문자열로
   그룹핑하면 같은 과일이 두 가지에 나타난다.
3. **지금 당장 등록 가능한 건 4건뿐이다**(`detail_status=done`). 트리를 붙여도 이 사실은
   가려지면 안 된다 — 카테고리를 골랐는데 "등록하기" 가 대부분 큐로 빠지는 게 정상 동작이다.

---

## 2. 🔴 그냥 붙이면 **언제나 0건**이 나온다 (이 설계의 절반)

도우미 트리 노드 id 는 전수 확인 결과 **401개 전부 `1000xxxx`** 다.
상품 행에 박힌 `naver_category_id` 는 **`5000xxxx`**(50002160, 50000960 …).
스냅샷에 `50002160` 은 **없다**(직접 확인).

```
트리 노드      10000107 여성의류 / 10007088 과일        ← 네이버 쇼핑 "메뉴" 분류
상품 cat_id    50002160, 50002162, 50000960 …          ← 네이버 "표준 상품" 분류
```

두 체계가 다르다. 그래서 트리 노드 id 를 그대로 `?category=` 에 넣으면 **결과가 항상 0건**이다.
트리를 그리는 일보다 이 간극을 메우는 일이 더 중요하다.

---

## 3. 결정 — 필터 축은 "수집 경로" 로 한다

| 후보 | 커버리지 | 문제 |
|---|---|---|
| **(A) 수집 트리 `1000xxxx`** | 전 행 채울 수 있음(수집 시점에 사실로 안다) | 상품 실제 분류보다 거칠다 |
| (B) 표준 상품분류 `5000xxxx` | 54/59 에 id 는 있음 | **이름 사전이 없다**(숫자뿐). 경로 문자열은 상세 확보분 4건에만 |
| (C) `category_path` 문자열 그룹핑 | 59/59 | 형식 2종 혼재 → 같은 분류가 갈라짐 |

→ **(A) 채택.** 이유는 세 가지다.
 · 상품이 어느 가지에서 왔는지는 **수집하는 순간 확실히 안다** — 추측이 아니다.
 · 트리 스냅샷(401 노드)이 이미 있어 오늘 바로 그릴 수 있다.
 · 관리자가 "신선식품 > 과일" 로 긁었으면 셀러도 그 이름으로 찾는다. 이름이 일치한다.

(B)의 `cat_id` 는 **버리지 않는다.** `std_category_id` 로 이름만 바꿔 보관한다 —
나중에 쿠팡 카테고리 자동매칭의 재료다. 지금 필터 축으로 쓰지 않을 뿐이다.
(C)는 카드 하단의 표시용 보조 라벨로만 남긴다.

---

## 4. 데이터

### 4.1 서버가 트리를 갖는다 (`sh_naver_categories`)

지금 트리는 **도우미 안에만** 있다. 셀러 PC 엔 도우미가 없으므로 셀러는 트리를 볼 수 없다.

```sql
CREATE TABLE IF NOT EXISTS sh_naver_categories (
  id          TEXT PRIMARY KEY,          -- 1000xxxx
  parent_id   TEXT REFERENCES sh_naver_categories(id) ON DELETE CASCADE,
  name        TEXT NOT NULL,
  depth       SMALLINT NOT NULL,         -- 1=대분류
  path_ids    TEXT[] NOT NULL,           -- 자기 포함 조상 배열
  path_names  TEXT NOT NULL,             -- '신선식품 > 과일'
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX ON sh_naver_categories(parent_id);
-- RLS: 읽기 = 로그인한 모두 / 쓰기 = 관리자 (sh_naver_sourcing_products 와 같은 규칙)
```

씨앗은 두 갈래로 넣는다.

- **웹 레포에 스냅샷 커밋** — `src/lib/megaload/naver-category-tree.json`(24KB, 도우미 것 복사).
  도우미가 한 번도 안 붙어도 트리는 뜬다. 이건 카탈로그가 "도우미 없이도 보이는 화면" 인 것과 같은 이유다.
- **관리자 sync** — `POST /api/megaload/naver-sourcing/categories/sync` 가 도우미
  `GET /naver-ingest/categories/export` 결과를 그대로 upsert. prewarm 이 소분류까지 읽고 나면 여기로 올린다.

### 4.2 상품에 조상 경로를 박는다

상위 노드를 한 번 눌러 하위 전부를 보려면, 행마다 조상 배열이 있어야 한다.
(`parent_id` 재귀 조회는 매 요청 401 노드를 타는 낭비다.)

```sql
ALTER TABLE sh_naver_sourcing_products
  ADD COLUMN IF NOT EXISTS cat_path_ids   TEXT[],   -- ['10007086','10007088']
  ADD COLUMN IF NOT EXISTS cat_path_names TEXT,     -- '신선식품 > 과일'
  ADD COLUMN IF NOT EXISTS std_category_id TEXT;    -- 기존 naver_category_id(5000xxxx) 이관
CREATE INDEX IF NOT EXISTS idx_naver_sourcing_cat_path
  ON sh_naver_sourcing_products USING gin (cat_path_ids);
```

- **상위 클릭 = 하위 전부** → `cat_path_ids @> ARRAY['10000107']` (GIN 인덱스로 즉답)
- **쓰기** — 조종석(`/megaload/naver-sourcing`)이 수집 결과를 POST 할 때 지금은 `catName` 문자열
  하나만 보낸다. `picked`(고른 경로 배열)를 그대로 함께 보내고, 서버가 두 컬럼을 채운다.
- **backfill** — 기존 55행의 `신선식품 > 과일` 을 **이름 매칭**으로 트리에 붙인다
  (`과일` = `10007088` 존재 확인). 못 붙는 행은 지우거나 숨기지 말고 **`미분류` 가지**로 모아
  트리에 그대로 보여 준다. 안 보이면 "내 상품이 사라졌다" 가 된다.

---

## 5. API

| 라우트 | 하는 일 |
|---|---|
| `GET  /api/megaload/naver-sourcing/categories` | `{ tree, counts }` — 노드별 `total` / `ready`(상세 확보) 수. **하위 합산값**을 준다 |
| `GET  /api/megaload/naver-sourcing/products?category=<id>&deep=1` | `deep=1` 이면 `cat_path_ids @>` 로 하위 포함 조회 |
| `POST /api/megaload/naver-sourcing/categories/sync` | 관리자 — 도우미 export 를 upsert |

카운트는 401개 노드를 매 요청 `group by` 하면 낭비다. 카탈로그는 하루 몇 번 변한다 →
**`s-maxage=300` 캐시 + 수집 POST 성공 시 revalidate**. "방금 긁은 게 안 보인다" 를 막는 건 후자다.

등록은 기존 `POST products/export`(ids 배열) 를 그대로 쓴다. 카테고리 통째 등록도 화면이 ids 를
모아 보내는 것뿐이다 — **서버 상한 200건**은 그대로 두고 UI 에 숫자로 명시한다.

---

## 6. 화면 — "잘 보이게"

```
┌ 전역 사이드바 w-64 ┬──────────────────────────────────────────────────────┐
│ (기존)             │ 네이버 소싱 카탈로그                                  │
│                    │ 홈 › 신선식품 › 과일            [검색] [정렬] [상세만] │
│                    ├───────────┬──────────────────────────────────────────┤
│                    │ 카테고리 ⌄ │ 과일   상품 55개 · 상세확보 4개           │
│                    │ [검색…]    │ [이 카테고리 전체 선택] [상세확보분만]    │
│                    │            ├──────────────────────────────────────────┤
│                    │ ▾ 신선식품 │ ┌────┐┌────┐┌────┐┌────┐┌────┐          │
│                    │   • 과일 55│ │카드││카드││카드││카드││카드│          │
│                    │     채소  0│ └────┘└────┘└────┘└────┘└────┘          │
│                    │ ▸ 여성의류 │                                          │
│                    │ ▸ 가공식품 │                                          │
└────────────────────┴───────────┴──────────────────────────────────────────┘
```

- **좌측 트리 패널 264px, sticky, 자체 스크롤, 접기 가능.** 접으면 격자가 6열로 넓어진다.
- **행 구성** `[▸] 이름 ······ 55` — 개수 pill 은 우측 정렬(숫자가 세로로 줄 맞아 스캔된다).
  상세 확보분이 있으면 pill 앞에 초록 점.
- **0개 가지는 숨기지 않는다.** 흐리게(gray-400) + 기본 접힘. 숨기면 "여성의류는 왜 없지" 가 된다.
- **선택 상태** 빨간 배경(#E31837 8%) + 좌측 3px 바 + 굵은 글씨. 조상은 자동 펼침.
- **트리 검색** 이름 매칭 노드만 남기고 조상을 자동으로 편다(401 노드는 스크롤로 못 찾는다).
- **브레드크럼은 항상 상단에** — 패널을 접어도 지금 어디인지 보인다. 각 조각 클릭 = 그 층으로.
- **카테고리 헤더 바** 이름 · 총 n개 · 상세확보 m개 · 두 버튼. 여기가 "골라서 등록" 의 입구다.
- **모바일** 상단 `카테고리` 버튼 → 좌측 드로어. 격자는 2열.
- **접근성** 트리는 `role="tree"/"treeitem"` + `aria-expanded`, 개수는 `aria-label="상품 55개"`.
  회색은 `text-gray-500` 이상(메모리의 WCAG 규칙 그대로).

---

## 7. 등록 흐름 (카테고리 → 등록)

1. 트리에서 카테고리 클릭 → 목록이 그 가지로 좁혀진다(브레드크럼 갱신).
2. `상세 확보분만` 을 켜면 **지금 바로 등록되는 것만** 남는다.
3. `이 카테고리 전체 선택` (200건 상한을 버튼 옆에 숫자로 표시).
4. `선택 n개 올인원으로 등록` → 기존 export → 도우미 → 올인원 그대로.
5. 상세 없는 건 **자동으로 큐에 등록**되고 준비되면 이어서 등록(이미 구현된 동작).
   지금은 55/59 가 여기로 간다 — 숨기지 말고 `n개는 준비 중입니다` 로 계속 보여 준다.

---

## 8. 단계 (각각 따로 배포 가능)

| 단계 | 내용 | 끝나면 보이는 것 |
|---|---|---|
| **P0** | 스냅샷 커밋 + `sh_naver_categories` + `GET categories` | 트리가 뜬다(카운트 없이) |
| **P1** | 좌측 트리 UI + 브레드크럼 + `?category=` 연결 | 카테고리로 목록이 좁혀진다 |
| **P2** | `cat_path_ids` 마이그레이션 + 조종석 경로 전송 + backfill | 상위 클릭 = 하위 전부, 카운트가 맞는다 |
| **P3** | 카테고리 단위 전체 선택·상세 통째 예약 | 카테고리 하나 = 등록 한 번 |

P0·P1 만 해도 화면은 쓸 만해진다. P2 가 없으면 **대분류를 눌러도 0건**이므로(2장 참조)
P1 단계에서는 트리의 말단(수집이 실제로 일어난 노드)만 클릭 가능하게 두고 상위는 펼침 전용으로 한다.

---

## 9. 리스크 · 미해결

- ~~**스냅샷이 depth 2 다.**~~ → **해결(2026-08-20)**. 소분류가 계속 0 이던 진짜 이유는 "아직 안
  읽어서" 가 아니라 **읽는 곳이 틀려서**였다: 소분류는 메뉴 페이지(`shopping.naver.com/ns/category/…`)
  에 아예 없고 **목록 페이지**(`search.shopping.naver.com/ns/category/…`, 로그인 필요)에만 있다.
  중분류 이하를 목록 페이지에서 읽도록 고치고(도우미 캐시 v4) 전량 재수집해 스냅샷을 갱신했다.
  갱신은 `scripts/build-naver-category-tree.mjs <export.json>` 이 도우미·웹 두 벌을 한 번에 만든다.
- **세분류(depth 4)는 여전히 없다.** 같은 방법으로 내려갈 수는 있지만 페이지가 수천 장이라
  몇 시간이 걸린다 — 필요해질 때 하면 된다(트리 코드는 깊이를 고정하지 않는다).
- **표준 분류(5000xxxx) 이름 사전이 없다.** 언젠가 이 축으로 갈아타려면 상세 확보분의
  `categoryPath` 문자열을 모아 id↔이름 사전을 만들어야 한다. 지금은 4건뿐이라 불가.
- **카운트 캐시 5분** — 수집 직후 revalidate 로 덮지만, 그래도 최대 5분 어긋날 수 있다.
- **export 200건 상한** — 카테고리가 200개를 넘으면 여러 번 눌러야 한다. UI 가 남은 수를 알려 준다.
- 카탈로그가 59행뿐이라 **트리 대부분이 0개로 보인다.** 이건 버그가 아니라 수집이 덜 된 것이고,
  화면이 그 사실을 정직하게 보여 주는 게 맞다.
