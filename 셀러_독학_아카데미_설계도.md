# 쿠팡 셀러 독학 아카데미 — 설계도

> **한 줄 정의**: 트레이너 없이 혼자서, 상담부터 상품등록·주문처리·CS까지
> **실제로 해내는 것**까지 데려다주는 시스템.
>
> **성공 기준**: 트레이너와 한 번도 통화하지 않은 사람이 30일 안에
> ① 쿠팡윙 입점 ② API 연동 ③ 상품 100개 등록 ④ 첫 주문 송장 등록 ⑤ 첫 문의 답변
> 을 **전부 시스템 판정으로 통과**한다.

---

## 0. 먼저 — "빈틈없이"를 정의한다

교육 콘텐츠가 실패하는 지점은 늘 같다. **설명은 있는데 "됐는지"를 아무도 안 봐준다.**
트레이너가 실제로 하던 일을 뜯어보면 다섯 가지다.

| # | 트레이너가 하던 일 | 시스템이 대신할 방법 | 난이도 |
|---|---|---|---|
| 1 | 지금 뭘 할 차례인지 알려줌 | 막·챕터·스텝 순서 + 이벤트 트리거 | 쉬움 (이미 있음) |
| 2 | 화면 보며 "거기 오른쪽 아래요" | 목업 + 핫스팟 + TTS 내레이션 + 영상 챕터 | 중간 |
| 3 | **됐는지 확인해줌** | **자동 판정 엔진 (쿠팡 API로 사실 조회)** | **핵심 · 어려움** |
| 4 | 막히면 답해줌 | 스텝별 트러블슈팅 → AI 상담 → 사람 호출 | 중간 |
| 5 | 사건 터지면 그때 알려줌 | 이벤트/마일스톤 트리거 (이미 있음) | 쉬움 |

**3번이 이 설계의 심장이다.** 나머지는 콘텐츠 작업이고, 3번만이 "트레이너 없이"를 성립시킨다.
설명만 잔뜩 있고 판정이 없으면 그건 그냥 문서고, 사람은 문서를 안 읽는다.

---

## 1. 재고 조사 — 이미 있는 것

새로 만들 필요가 없는 것들. **이걸 모르고 새로 만들면 세 번째 교육 시스템이 된다.**

### 1-1. 커리큘럼 엔진 (있음 · 재사용)

`supabase/migration_pt_education.sql`

- `pt_education_modules` — 17개 모듈. `category`(upfront/operation/event/milestone/reactive/optional),
  `sub_modules` JSONB, `trigger_condition` JSONB, `display_order`, `trigger_hint`
- `pt_education_progress` — 학생별 상태 5종(locked/triggered/in_progress/completed/needs_review),
  `sub_progress`, `resume_point`
- `ensure_pt_education_progress()` RPC — 가입 시 자동 시드
- `migration_pt_education_triggers.sql` — `sh_orders`/`sh_products` INSERT 시 모듈 자동 트리거

**이미 시드된 17개 모듈**: 사업자등록 / 통신판매업 / 건기식 수료증 / 쿠팡윙 입점 / API 연동 /
구글드라이브 연동 / 상품 업로드 설명 / 상품 업로드 팁 / AI광고 관리 / 주문 취소 / 주문처리 /
반품처리 / 브랜드사 대응 / 쿠팡 대응 / 프로모션 쿠폰 / 현대카드 / 사업자 명의 폰·네이버

> ⚠️ **치명적 한계**: `/api/education/me` 는 **읽기 전용**이다. 진행 상태를 쓰는 경로는
> `/api/admin/education/students/**` 뿐 — 즉 **트레이너만 "완료"를 찍을 수 있다.**
> 이게 트레이너 의존성의 실체다. 여기를 끊는 게 Phase 0.

### 1-2. 콘텐츠 (있음 · 재사용)

| 자산 | 위치 | 규모 | 쓸모 |
|---|---|---|---|
| 실무 가이드 | `src/lib/data/guides.ts` | 32편 / 9카테고리 / 4,560줄 | 스텝 본문의 원천 |
| 공개 아티클 | `src/lib/data/guide-articles.ts` | 751줄 | SEO 유입 + 보충 설명 |
| 기능 튜토리얼 | `src/lib/data/feature-tutorials.ts` | 15개 · XP 포함 | 앱 내 기능 안내 |
| AI 상담 KB | `src/lib/assistant/kb/*` | 3,669줄 (operator/pages/practical/troubleshooting) | 막혔을 때의 답 |
| 스텝 이미지 | `guide_step_images` 테이블 + `GuideImageWithHotspots.tsx` | — | **목업 + 핫스팟** |
| 교육 영상 | `training_videos` 테이블 (YouTube 임베드) | — | 영상 챕터 |

`GuideStep` 타입이 이미 우리가 필요한 걸 거의 다 갖고 있다:
`title / description / detailedInstructions[] / images[] / externalLink / tip / warning / copyableTemplates[]`

### 1-3. 게이미피케이션 씨앗 (있음 · 절반)

- `src/components/tutorial/` 9개 컴포넌트 — `FeatureTutorial`, `TutorialHub`,
  `TutorialCelebration`, `WelcomeTutorial` … 15개 페이지에서 이미 사용 중
- XP 개념 존재 (`tutorial.xp`, `TOTAL_TUTORIAL_XP`)
- `/my/arena` — 익명 셀러 랭킹 (상품 등록 수 기준, 쿠팡 API 동기화)
- `framer-motion` 설치됨

> ⚠️ **치명적 한계**: `src/lib/utils/tutorial-progress.ts` 가 **전부 localStorage**다.
> 기기 바꾸면 사라지고, 서버가 모르니 보상·랭킹·판정 어디에도 못 쓴다.

### 1-4. 자동 판정의 재료 (있음 · 미사용)

`src/lib/megaload/adapters/coupang.adapter.ts` — **이게 판정 엔진의 전부다.**

| 메서드 | 판정할 수 있는 것 |
|---|---|
| `testConnection()` | API 연동 성공 |
| `getOutboundShippingPlaces()` | 출고지 등록 여부 |
| `getReturnShippingCenters()` | 반품지 등록 여부 |
| `getProducts({status})` | 등록 상품 수 · 승인 상태 |
| `getProductDetail()` / `getCoupangSaleTruth()` | 실제 판매중인지 |
| `getOrders({startDate,endDate})` | 첫 주문 발생 · 주문 상태 |
| `registerInvoice()` | 송장 등록 성공 |
| `confirmOrder()` / `cancelOrder()` | 주문 확인 · 취소 처리 |
| `getReturnRequests()` / `registerReturnInvoice()` | 반품 접수 · 회수 처리 |
| `getInquiries()` / `answerInquiry()` | 고객 문의 · 답변 |
| `getSettlements()` | 정산 발생 |

### 1-5. 원자료 (있음 · 미가공)

`G:\내 드라이브\landingpage\coupanglanding\자료\`

- **`회원등록용 텍스트.txt`** — `등록자료.mp4` 전사본, **타임코드 포함**.
  코치가 수강생 화면을 보며 진행한 실제 1:1 등록 교육 통화 전문.
  → 영상 챕터 분할과 TTS 내레이션 초안이 **여기서 그대로 나온다.**
- `등록자료.mp4` (120MB) / `등록자료_vrew.mp4` (48MB)
- `쿠팡 상품등록 실무교육.pdf` (19p) — 위 통화를 7단계로 정리한 슬라이드
- `쿠팡 위탁판매 교육.pdf` (20p) — 구조·정산·마진 입문
- `내용증명 법무법인\` — 상표권 내용증명 실물 · 법무법인 답변서 · 대법원 판례

---

## 2. 빠진 것 — 이 설계가 만드는 것

| # | 빠진 것 | 왜 필요한가 |
|---|---|---|
| A | **자동 판정 엔진** | 트레이너 대체의 전부 |
| B | **서버 진행 상태** | localStorage → DB. 보상·랭킹·판정의 전제 |
| C | **TTS 내레이션** | "설명 안 해주더라도" |
| D | **영상 챕터 매핑** | 통짜 영상은 아무도 안 본다. 스텝당 40~90초 |
| E | **목업 + 핫스팟** | 글로 "오른쪽 아래"는 안 통한다 |
| F | **게이미피케이션 원장** | XP/레벨/뱃지/스트릭 — 실제 성취에 연동 |
| G | **한 줄기 커리큘럼** | 지금은 17개 모듈이 평평하게 나열됨. 막(Act) 구조 필요 |
| H | **막혔을 때의 출구** | 3단 에스컬레이션. 없으면 이탈 |

---

## 3. 정보 구조 — 5막(Act)

지금의 17개 평면 모듈을 **이야기 순서**로 재배열한다.
사람은 카테고리로 배우지 않고 **자기가 겪는 순서**로 배운다.

```
Act 0 · 진단 ─── "나한테 맞나?"          [상담 대체]
   └ 0-1 셀러 적성 진단 (5문항)
   └ 0-2 예상 수익·소요시간 시뮬레이터
   └ 0-3 시작 전 체크리스트 (자본·시간·사업자)

Act 1 · 개업 ─── "팔 수 있는 상태 만들기"   [upfront 6모듈]
   └ 1-1 사업자등록          1-2 통신판매업 신고
   └ 1-3 건기식 수료증        1-4 쿠팡윙 입점
   └ 1-5 API 연동            1-6 출고지·반품지 등록
   └ 1-7 구글드라이브 연동     1-8 도우미(데스크톱) 설치

Act 2 · 등록 ─── "첫 상품을 올린다"        [핵심 · 원자료 영상 있음]
   └ 2-1 소싱: 무엇을 팔까     2-2 카탈로그에서 고르기
   └ 2-3 폴더 스캔 / 카탈로그 수동등록
   └ 2-4 카테고리 자동매칭 기다리기
   └ 2-5 전체 검증 + 이미지 사전 업로드 (프리플라이트)
   └ 2-6 필수 속성 채우기 (중량·용량·수량)
   └ 2-7 상세페이지 검수      2-8 등록 실행 · 승인 확인

Act 3 · 주문 ─── "돈이 들어온다"          [event 트리거]
   └ 3-1 첫 주문 알림 읽기     3-2 발주서 확인 · 주소/안심번호
   └ 3-3 소싱처에 발주        3-4 송장 등록
   └ 3-5 품절 대응 (출고중지)   3-6 주문 취소 처리
   └ 3-7 배송 지연 대응

Act 4 · CS ────── "사고가 난다"           [event + reactive]
   └ 4-1 고객 문의 답변       4-2 반품 접수 처리
   └ 4-3 자체수거 vs 회수신청   4-4 진상 고객 응대
   └ 4-5 브랜드사 내용증명 대응  4-6 쿠팡 지재권·상표권 대응
   └ 4-7 판매자 점수·페널티 관리

Act 5 · 성장 ─── "늘린다"                [milestone]
   └ 5-1 정산 구조 이해       5-2 부가세 신고
   └ 5-3 AI광고 (광고비/ROAS/최적화)
   └ 5-4 프로모션 쿠폰        5-5 아이템위너 전략
```

**매핑 원칙**: 기존 `pt_education_modules.key` 를 **그대로 쓴다.** 막 구조는 스텝 쪽에
`act` 필드로 얹는다. 기존 트레이너 화면(`/my/trainer`, 관리자 교육 현황판)이 계속 돌아가야 한다.

---

## 4. 스텝 스키마 — "빈틈없이"의 실제 정의

스텝 하나가 아래 **9칸을 전부 채우지 못하면 발행하지 않는다.** 이게 품질 게이트다.

```ts
// src/lib/data/academy/types.ts
export interface AcademyStep {
  key: string;                 // 'act2-05-preflight'
  act: 0 | 1 | 2 | 3 | 4 | 5;
  moduleKey?: string;          // 기존 pt_education_modules.key 연결
  order: number;

  // ── ① 목표 ── 한 줄. "이 스텝이 끝나면 무엇이 참이 되는가"
  goal: string;                // "프리플라이트가 초록으로 다 찬다"

  // ── ② 왜 ── 건너뛰면 무슨 일이 나는지. 이유 없는 지시는 안 지켜진다
  why: string;

  // ── ③ 예상 소요 ──
  estimatedSec: number;

  // ── ④ 영상 챕터 ── 통짜 금지. 스텝당 40~90초
  video?: { youtubeId: string; startSec: number; endSec: number; transcript: string };

  // ── ⑤ 목업 + 핫스팟 ── 실제 화면 캡처 위에 좌표로 찍는다
  mockup?: {
    imageUrl: string;
    capturedAt: string;        // 캡처 일자 — 쿠팡 UI 바뀌면 만료 판정에 쓴다
    hotspots: { x: number; y: number; w: number; h: number; label: string; order: number }[];
  };

  // ── ⑥ 내레이션 ── TTS 원문. 문장 배열이어야 하이라이트 동기화가 된다
  narration: string[];

  // ── ⑦ 실제로 할 일 ── 딥링크 + 동작 지시
  actions: {
    label: string;
    href?: string;             // 내부 딥링크 또는 쿠팡윙 URL
    external?: boolean;
    copyable?: { label: string; text: string };   // 붙여넣을 문구(CS 템플릿 등)
  }[];

  // ── ⑧ 합격 판정 ── ★ 이 설계의 심장
  verify: VerifySpec;

  // ── ⑨ 막히면 ── 예상 실패 목록. 없으면 AI 상담으로, 그래도 안 되면 사람
  troubleshoot: { symptom: string; cause: string; fix: string }[];

  // 보상
  xp: number;
  badgeKey?: string;
}
```

---

## 5. 합격 판정 엔진 — 3단계

**원칙: 가능한 한 L1. L1이 불가능할 때만 L2. L3는 최후.**
사람에게 "다 했어요"를 묻는 순간 그건 판정이 아니라 자기신고다.

```ts
export type VerifySpec =
  // L1 — 사실 조회. 쿠팡 API / 우리 DB 로 확인한다. 사람 개입 0
  | { level: 1; probe: ProbeKey; params?: Record<string, unknown>;
      pass: (r: unknown) => boolean; hint: string }

  // L2 — 증빙 제출. 이미지 업로드 → 규칙/OCR 판정
  | { level: 2; accept: 'image' | 'pdf'; extract: ExtractKey;
      pass: (x: Extracted) => boolean; retentionDays: number }

  // L3 — 자가 확인 + 퀴즈. 자동화가 원천적으로 불가능한 것만
  | { level: 3; checklist: string[]; quiz: { q: string; choices: string[]; answer: number; why: string }[] };
```

### 5-1. 스텝별 판정 매핑 (전수)

| 스텝 | 레벨 | 판정 방법 | 근거 |
|---|:--:|---|---|
| 1-1 사업자등록 | L2 | 등록증 업로드 → 사업자번호 추출 → **국세청 상태조회 API** 대조 | ※ 공공데이터 API 사용 가능 여부 **확인 필요** |
| 1-2 통신판매업 신고 | L2 | 신고증 업로드 → 신고번호 형식 검증 (+ 공정위 조회 검토) | ※ 조회 자동화 **확인 필요** |
| 1-3 건기식 수료증 | L2 | 수료증 이미지 업로드 → 발급기관·일자 추출 | — |
| 1-4 쿠팡윙 입점 | L1 | 1-5 통과가 곧 증명 (API 키는 윙 계정에서만 나온다) | — |
| 1-5 API 연동 | **L1** | `adapter.testConnection()` | ✅ 확실 |
| 1-6 출고지·반품지 | **L1** | `getOutboundShippingPlaces().length > 0` ∧ `getReturnShippingCenters().length > 0` | ✅ 확실 |
| 1-7 구글드라이브 | L1 | 로컬 도우미 폴더 스캔 성공 이벤트 | ✅ |
| 1-8 도우미 설치 | **L1** | `findHelper()` 응답 | ✅ 확실 |
| 2-2 카탈로그 선택 | L1 | 카탈로그 `picked` 이벤트 기록 | ✅ |
| 2-3 스캔/수동등록 | L1 | `startFromScannedResult` 진입 (step 2 도달) | ✅ |
| 2-5 프리플라이트 | **L1** | 이미지 사전업로드 캐시 채워짐 + 검증 통과 | ✅ 확실 |
| 2-8 등록 실행 | **L1** | `getProducts()` 개수 증가 ≥ 1 | ✅ 확실 |
| 2-8b 승인 확인 | **L1** | `getProductDetail().status == 승인완료` | ✅ 확실 |
| 3-2 발주서 확인 | **L1** | `getOrders(ACCEPT)` → `confirmOrder()` 호출 성공 | ✅ 확실 |
| 3-4 송장 등록 | **L1** | `registerInvoice()` 성공 ∧ 주문 상태 전이 | ✅ 확실 |
| 3-5 품절 대응 | **L1** | `stopItemSale()` 성공 | ✅ 확실 |
| 3-6 주문 취소 | **L1** | `cancelOrder()` 성공 | ✅ 확실 |
| 4-1 문의 답변 | **L1** | `getInquiries()` 미답변 → `answerInquiry()` 성공 | ✅ 확실 |
| 4-2 반품 처리 | **L1** | `getReturnRequests()` 상태 전이 | ✅ 확실 |
| 4-4 진상 응대 | L3 | 시나리오 퀴즈 (실제 사례 기반) + CS 템플릿 사용 기록 | — |
| 4-5 내용증명 대응 | L3 | 시나리오 퀴즈 (`내용증명 법무법인` 실물 자료 기반) | — |
| 4-6 지재권·상표권 | L3 | 시나리오 퀴즈 + 판례 읽음 확인 | — |
| 4-7 판매자 점수 | **L1** | 판매자 점수 조회 + 임계치 | ※ 어댑터 메서드 **추가 필요** |
| 5-1 정산 구조 | **L1** | `getSettlements()` 1건 이상 | ✅ 확실 |
| 5-2 부가세 신고 | L3 | 체크리스트 + 퀴즈 | — |
| 5-3 AI광고 | L1 | 광고 API 캠페인 존재 (`/api/megaload/ads`) | ✅ |

> **판정 결과 집계**: L1 **18스텝**, L2 3스텝, L3 5스텝.
> 즉 **핵심 경로(Act 1~3)는 거의 전부 자동 판정된다.** 트레이너 없이가 성립한다.

### 5-2. 판정 엔진의 운영 규칙

1. **쿨다운** — 스텝당 재판정 최소 간격 20초. 쿠팡 API 호출량 보호.
2. **캐시** — 같은 프로브는 60초 캐시. 한 화면에서 여러 스텝이 같은 프로브를 쓴다.
3. **오판 대비** — 3회 연속 실패 시 **"수동 통과 요청"** 버튼 노출.
   요청은 `needs_review` 로 큐에 쌓이고, 관리자가 나중에 확인한다.
   > 자동 판정이 틀렸는데 길이 막히면 신뢰가 한 번에 무너진다. 우회로는 반드시 둔다.
4. **원장 기록** — 판정 성공/실패를 전부 append-only 로 남긴다.
   나중에 "어느 스텝에서 사람이 죽는가"가 이 데이터에서 나온다.
5. **역행 금지** — 한번 통과한 스텝은 다시 잠기지 않는다 (상품을 지워도 2-8은 통과 상태 유지).

---

## 6. TTS 설계

> 제약: **프로덕션에 `OPENAI_API_KEY`/`GEMINI_API_KEY` 가 없다.**
> 런타임이 서버 LLM/TTS 키에 의존하면 그냥 안 돌아간다. → **정적 파일 + 브라우저 폴백** 구조로 간다.

### 6-1. 2단 구조

```
1순위) 사전 생성 오디오
   narration[] → 해시 → mp3 → Supabase Storage  academy-tts/{stepKey}/{hash}.mp3
   · 생성은 관리자가 오프라인 배치로 (스크립트 1회 실행)
   · 런타임 비용 0, 품질 일정, 문장별 타임스탬프 동봉(JSON)
   · GET /api/academy/tts/[stepKey] → 있으면 302, 없으면 204

2순위) 브라우저 SpeechSynthesis (204 응답 시 자동 폴백)
   · window.speechSynthesis + ko-KR voice
   · 비용 0, 오프라인 동작, 품질은 낮지만 "설명이 없는 것"보단 낫다
   · onboundary 이벤트로 문장 하이라이트 동기화
```

### 6-2. 재생 UI 요구사항

- 문장 단위 하이라이트 (읽는 곳이 파랗게)
- 배속 0.75 / 1.0 / 1.25 / 1.5
- **자막 항상 표시 가능** (접근성 — 소리 못 켜는 환경이 실제로 많다)
- 자동재생은 **기본 꺼짐**. 첫 스텝에서 한 번 물어보고 기억한다
- 영상과 동시 재생 금지 (영상 재생 시 내레이션 일시정지)

### 6-3. 내레이션 작성 규칙

- 한 문장 = 한 동작. "그리고", "그러면서" 로 잇지 않는다
- 위치는 **화면 기준**으로. "오른쪽 아래 파란 버튼" (○) / "폴더 선택 버튼" (△)
- 기다림은 **미리 말한다**. "여기서 30초쯤 멈춥니다. 고장 아닙니다"
  → 전사본에 이 대사가 실제로 있다: *"컴퓨터나 노트북마다 사양이 좀 달라서 걸리는 시간이 다르거든요"*
- 실패 신호도 말한다. *"프리플라이트가 안 차면 쿠팡 쪽에 뭔가 걸린 겁니다"*

---

## 7. 영상 설계

### 7-1. 원자료 → 챕터

`등록자료.mp4` + `회원등록용 텍스트.txt`(타임코드 전사본)를 **스텝 단위로 자른다.**

전사본에서 이미 확인된 구간 예시:

| 타임코드 | 내용 | 매핑 스텝 |
|---|---|---|
| 00:00:05~00:01:10 | 상품등록 → 폴더 선택 → 구글드라이브 → 폴더 고르기 | 2-3 |
| 00:01:07~00:02:37 | 스캔 대기 · 사양별 소요시간 · 카테고리 자동매칭 | 2-4 |
| 00:02:37~00:03:41 | 전체검증 + 이미지 사전업로드 = 프리플라이트 설명 | 2-5 |
| 00:03:43~00:05:09 | 소싱처 원가 확인 → 중량·용량·수량 입력 | 2-6 |

**작업**: 전사본 전체(15KB)를 훑어 40~90초 챕터로 분할 → 스텝에 `{startSec, endSec}` 부여.
영상은 YouTube **미등록(unlisted)** 업로드 → 기존 `training_videos` 테이블 재사용
(`start_sec`/`end_sec`/`step_key` 컬럼만 추가).

### 7-2. 신규 촬영이 필요한 구간

원자료는 **Act 2(등록)만** 덮는다. 나머지는 화면 녹화가 필요하다.

- Act 1 (입점·연동) — 쿠팡윙 가입·API 발급 화면 (민감정보 마스킹 필수)
- Act 3 (주문처리) — 실제 발주서·송장 등록
- Act 4 (CS·반품) — 문의 답변·반품 접수 화면

> **중간 대안**: 영상이 없는 스텝은 **목업+핫스팟+TTS 만으로도 발행 가능**하게 설계한다.
> `video` 는 optional. 영상 없다고 스텝을 못 내면 영원히 못 낸다.

---

## 8. 게이미피케이션 설계

### 8-1. 대원칙 — 가짜 보상 금지

> **XP는 "읽음"이 아니라 "해냄"에 준다.**
> 슬라이드를 넘겼다고 주는 XP는 사람을 속이는 것이고, 속은 사람은 두 번 안 온다.
> 그래서 XP 지급은 **판정 통과 시에만** 일어난다 (L1/L2/L3 무관).

### 8-2. 레벨 — 셀러 단계 (실제 상태에 연동)

| 레벨 | 이름 | 승급 조건 (전부 자동 판정) |
|:--:|---|---|
| 1 | 예비 셀러 | 가입 |
| 2 | 입점 셀러 | API 연동 + 출고지·반품지 등록 완료 |
| 3 | 첫 상품 셀러 | 상품 1건 승인 완료 |
| 4 | 판매 셀러 | 첫 주문 송장 등록 완료 |
| 5 | 운영 셀러 | 첫 문의 답변 + 첫 반품 처리 완료 |
| 6 | 정산 셀러 | 첫 정산 발생 |
| 7 | 백개 셀러 | 상품 100개 |
| 8 | 천개 셀러 | 상품 1,000개 (기존 milestone 트리거와 일치) |
| 9 | 광고 셀러 | 광고 캠페인 운영 + ROAS 판정 |
| 10 | 베테랑 | 월 매출 임계치 (아레나 상위) |

### 8-3. 뱃지(마크) — 획득 조건은 전부 검증 가능해야 한다

| 뱃지 | 조건 | 성격 |
|---|---|---|
| 🔌 첫 연결 | API 연동 성공 | 진도 |
| 📦 첫 등록 | 상품 1건 승인 | 진도 |
| 🚚 첫 송장 | 송장 등록 성공 | 진도 |
| 💬 첫 답변 | 고객 문의 답변 | 진도 |
| ↩️ 첫 반품 | 반품 처리 완료 | 진도 |
| 💰 첫 정산 | 정산 1건 | 진도 |
| ⚡ 당일 처리 | 주문 접수 → 송장 등록 24시간 이내 × 7일 | **습관** |
| 🎯 무지연 | 배송 지연 0건 30일 | **습관** |
| 🛡️ 무페널티 | 판매자 점수 유지 90일 | **습관** |
| 🧠 사고 대응 | 지재권/내용증명 시나리오 퀴즈 만점 | 지식 |
| 🌙 한밤의 셀러 | 자정~새벽 주문 처리 | 재미 |
| 🔥 7일 연속 | 7일 연속 운영 행동 | 스트릭 |

> **습관 뱃지가 핵심이다.** 진도 뱃지는 한 번 받으면 끝이고, 습관 뱃지가 사람을 남긴다.
> 그리고 습관 뱃지의 조건은 전부 쿠팡 API로 검증된다 — 자기신고가 아니다.

### 8-4. 스트릭 — "접속"이 아니라 "운영"

연속 **로그인**은 의미 없는 지표다. 아래 중 하나라도 하면 그날은 채워진다.

- 주문 처리 (확인·송장)
- 문의 답변
- 상품 등록 (1건 이상)
- 스텝 1개 통과

주말·공휴일은 **프리즈**(스트릭 유지, 카운트 안 함). 실제 셀러는 주말에도 주문이 들어오므로
"주말에 처리하면 보너스" 로 뒤집는다.

### 8-5. 퀘스트

- **일일**: "오늘 들어온 주문 전부 송장 등록" / "미답변 문의 0건 만들기"
- **주간**: "상품 20개 추가" / "반품 요청 전부 처리"
- **스토리**: 각 Act 완주

퀘스트는 **실제 밀린 일**에서 자동 생성된다 (미답변 문의 수, 미처리 주문 수).
할 일이 없으면 퀘스트도 없다 — 없는 일을 만들어내지 않는다.

### 8-6. 아레나 연동 (기존 `/my/arena` 확장)

지금은 상품 등록 수 단일 랭킹. 여기에 부문을 추가한다 — 단, **전부 익명 유지**.

- 📦 등록왕 (기존)
- ⚡ 처리속도 (주문→송장 중앙값)
- 💬 응답속도 (문의→답변 중앙값)
- 🎓 진도 (아카데미 완주율)

---

## 9. 데이터 모델

### 9-1. 콘텐츠는 코드, 진행은 DB

> 이 레포의 기존 관행(`guides.ts`, `feature-tutorials.ts`)을 따른다.
> 콘텐츠를 DB에 넣으면 타입 검사·코드리뷰·git diff 를 전부 잃는다.
> 나중에 관리자 편집이 필요해지면 **오버라이드 테이블**만 얹는다.

```
src/lib/data/academy/
  types.ts          // AcademyStep, VerifySpec, ProbeKey …
  act0-diagnose.ts
  act1-open.ts
  act2-register.ts   ← 원자료 영상 있는 구간. 여기부터 쓴다
  act3-orders.ts
  act4-cs.ts
  act5-growth.ts
  index.ts          // ACADEMY_STEPS, getStep(), getAct()
```

### 9-2. 새 테이블 (`supabase/migration_academy.sql`)

```sql
-- 스텝 진행 (localStorage 대체)
CREATE TABLE academy_progress (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  step_key TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'locked'
    CHECK (status IN ('locked','available','in_progress','verifying','passed','needs_review')),
  verify_level SMALLINT,              -- 어떤 레벨로 통과했는지 (감사용)
  verify_payload JSONB,               -- 판정 근거 스냅샷 (뭘 보고 통과시켰나)
  attempts INTEGER NOT NULL DEFAULT 0,
  last_attempt_at TIMESTAMPTZ,
  evidence_path TEXT,                 -- L2 증빙 (Storage 경로)
  passed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (user_id, step_key)
);

-- XP 원장 (append-only — 잔액을 직접 쓰지 않는다. 언제나 재계산 가능해야 한다)
CREATE TABLE academy_xp_ledger (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  amount INTEGER NOT NULL,
  reason TEXT NOT NULL,               -- 'step_passed' | 'badge' | 'quest' | 'streak'
  ref_key TEXT NOT NULL,              -- step_key / badge_key / quest_key
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (user_id, reason, ref_key)   -- ★ 중복 지급 원천 차단
);

-- 뱃지
CREATE TABLE academy_badges (
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  badge_key TEXT NOT NULL,
  awarded_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  proof JSONB,                        -- 무엇을 보고 줬는가
  PRIMARY KEY (user_id, badge_key)
);

-- 스트릭
CREATE TABLE academy_streaks (
  user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  current_days INTEGER NOT NULL DEFAULT 0,
  best_days INTEGER NOT NULL DEFAULT 0,
  last_active_date DATE,
  freeze_used_this_week SMALLINT NOT NULL DEFAULT 0
);

-- 판정 로그 (어느 스텝에서 사람이 죽는가 — 콘텐츠 개선의 유일한 근거)
CREATE TABLE academy_verify_log (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID NOT NULL,
  step_key TEXT NOT NULL,
  level SMALLINT NOT NULL,
  passed BOOLEAN NOT NULL,
  probe TEXT,
  error TEXT,
  duration_ms INTEGER,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX ON academy_verify_log (step_key, passed, created_at DESC);

-- TTS 자산
CREATE TABLE academy_tts_assets (
  step_key TEXT NOT NULL,
  script_hash TEXT NOT NULL,          -- narration 해시 — 대본 바뀌면 자동 무효
  voice TEXT NOT NULL,
  audio_path TEXT NOT NULL,
  marks JSONB,                        -- 문장별 타임스탬프
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  PRIMARY KEY (step_key, script_hash, voice)
);
```

**RLS**: 전부 `user_id = auth.uid()` 본인 것만. 쓰기는 서비스 클라이언트(API 라우트)만.
증빙 파일(`evidence_path`)은 **비공개 버킷** + 서명 URL. 사업자등록증은 민감정보다.

### 9-3. 기존 `pt_education_progress` 와의 관계

**양방향 동기화하지 않는다.** 한쪽이 진실이다.

- `academy_progress` = 셀러 본인의 자율 학습 진행 (**진실**)
- `pt_education_progress` = 트레이너 화면용 요약 (academy 통과 시 **단방향 미러링**)

스텝 통과 → 해당 `moduleKey` 의 모든 스텝이 통과면 → `pt_education_progress.status = 'completed'`
로 밀어준다. 트레이너 화면은 손대지 않아도 저절로 채워진다.

---

## 10. API 설계

| 엔드포인트 | 하는 일 |
|---|---|
| `GET /api/academy/me` | 전체 트리 + 내 진행 + XP/레벨/뱃지/스트릭 (1콜) |
| `POST /api/academy/steps/[key]/start` | `in_progress` 전환 |
| `POST /api/academy/steps/[key]/verify` | **판정 실행** (L1 프로브 / L3 퀴즈 채점) |
| `POST /api/academy/steps/[key]/evidence` | L2 증빙 업로드 → 추출 → 판정 |
| `POST /api/academy/steps/[key]/review-request` | 3회 실패 후 수동 통과 요청 |
| `GET /api/academy/tts/[key]?voice=` | mp3 302 / 없으면 204 (브라우저 폴백 신호) |
| `POST /api/academy/ask` | 스텝 컨텍스트 + KB 주입한 AI 상담 (기존 assistant 재사용) |
| `GET /api/academy/quests` | 실제 밀린 일에서 생성된 오늘의 퀘스트 |
| `POST /api/admin/academy/tts/build` | 관리자 배치: narration → mp3 생성 |

**판정 라우터** — `src/lib/academy/verify/` 에 프로브를 등록제로 둔다.

```ts
// probes/index.ts
export const PROBES: Record<ProbeKey, Probe> = {
  'coupang.connection':   async (ctx) => adapter(ctx).testConnection(),
  'coupang.shipping':     async (ctx) => ({ out: await adapter(ctx).getOutboundShippingPlaces(),
                                            ret: await adapter(ctx).getReturnShippingCenters() }),
  'coupang.products':     async (ctx, p) => adapter(ctx).getProducts({ status: p?.status, size: 1 }),
  'coupang.orders':       async (ctx, p) => adapter(ctx).getOrders({ ...dateRange(p), status: p?.status }),
  'coupang.inquiries':    async (ctx, p) => adapter(ctx).getInquiries(dateRange(p)),
  'coupang.returns':      async (ctx, p) => adapter(ctx).getReturnRequests(dateRange(p)),
  'coupang.settlements':  async (ctx, p) => adapter(ctx).getSettlements(dateRange(p)),
  'helper.online':        async ()      => findHelper().then(Boolean),
  'db.productCount':      async (ctx)   => countProducts(ctx.userId),
  // …
};
```

프로브는 **읽기 전용만**. 판정이 사용자의 쿠팡 계정을 바꾸는 일은 절대 없어야 한다.

---

## 11. 화면 설계 (목업)

### 11-1. `/my/academy` — 지도

평평한 목록이 아니라 **길**로 보여준다. 지금 어디 있고 다음이 뭔지가 한눈에 보여야 한다.

```
┌──────────────────────────────────────────────────────────────┐
│  🎓 셀러 아카데미            Lv.3 첫 상품 셀러   1,240 XP    │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░  14/31 스텝    🔥 5일 연속          │
├──────────────────────────────────────────────────────────────┤
│  오늘의 퀘스트                                                │
│  ☐ 어제 들어온 주문 3건 송장 등록          +30 XP            │
│  ☐ 미답변 문의 1건 답변                    +20 XP            │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│   Act 1 · 개업 ✅ 완료                                        │
│   ●━━●━━●━━●━━●━━●━━●━━●                                    │
│                                                               │
│   Act 2 · 등록  ▓▓▓▓▓▓░░  6/8                                │
│   ●━━●━━●━━●━━●━━●━━◉━━○                                    │
│                      ↑ 지금 여기                              │
│                      2-7 상세페이지 검수                      │
│                      [ 이어서 하기 ]                          │
│                                                               │
│   Act 3 · 주문  🔒  첫 주문이 들어오면 열립니다               │
│   ○━━○━━○━━○━━○━━○━━○                                       │
│                                                               │
│   Act 4 · CS    🔒  첫 문의가 들어오면 열립니다               │
│   Act 5 · 성장  🔒  상품 100개부터                            │
│                                                               │
├──────────────────────────────────────────────────────────────┤
│  🏅 뱃지  🔌 📦 ⚡ ▫️ ▫️ ▫️ ▫️ ▫️        [ 뱃지장 ]          │
└──────────────────────────────────────────────────────────────┘
```

> **잠금 표시가 중요하다.** "🔒 첫 주문이 들어오면 열립니다" 는 벌이 아니라 **약속**이다.
> 지금 못 배우는 게 아니라 **그때 배우면 된다**고 말해준다. (기존 `trigger_hint` 필드가 이 문구다)

### 11-2. 스텝 플레이어 — 3분할

```
┌────────────────────────────────────┬─────────────────────────┐
│  2-5 · 프리플라이트 돌리기         │  🔊 내레이션            │
│                                    │  ──────────────────     │
│  ┌──────────────────────────────┐  │  ▶ 1.0x  [자막 ON]     │
│  │                              │  │                         │
│  │   [영상 00:12 / 01:04]       │  │  ▸ 전체 검증 플러스     │
│  │   또는 목업 + 핫스팟         │  │    이미지 사전 업로드을 │
│  │                              │  │    누릅니다.            │
│  │      ①───────┐               │  │  ▪ 이건 미리 API를 쏴   │
│  │              ▼               │  │    보는 겁니다.         │
│  │         [전체 검증 +      ]  │  │  ▪ 안 차면 쿠팡 쪽에    │
│  │         [이미지 사전 업로드] │  │    뭔가 걸린 겁니다.    │
│  │                              │  │                         │
│  └──────────────────────────────┘  │  ──────────────────     │
│                                    │  🎯 목표                │
│  ▸ 지금 할 일                      │  게이지가 전부 초록     │
│    [ 상품등록 화면 열기 ↗ ]        │                         │
│                                    │  ⏱ 예상 2분             │
│  ⚠️ 카테고리 자동매칭이 끝나기      │  ⭐ +50 XP              │
│     전에 누르면 오류가 납니다.     │                         │
├────────────────────────────────────┴─────────────────────────┤
│  ✅ 다 했나요?                                                │
│  ┌───────────────────────────────────────────────────────┐   │
│  │  [ 확인하기 ]   ← 누르면 시스템이 직접 봅니다          │   │
│  │                  (사전업로드 캐시 + 검증 결과 조회)    │   │
│  └───────────────────────────────────────────────────────┘   │
│  ❓ 안 되나요?  ▸ 게이지가 안 찹니다  ▸ 오류가 뜹니다        │
│                 ▸ 그래도 안 되면 [ AI에게 묻기 ]             │
└──────────────────────────────────────────────────────────────┘
```

### 11-3. 판정 결과

```
  통과                                실패
┌─────────────────────────┐   ┌─────────────────────────────────┐
│   ✅  확인했습니다        │   │   ❌  아직 안 된 것 같습니다     │
│                         │   │                                 │
│   상품 3건이 쿠팡에      │   │   쿠팡에 등록된 상품이           │
│   등록된 걸 확인했습니다 │   │   0건으로 조회됩니다.            │
│                         │   │                                 │
│   +50 XP   🏅 첫 등록    │   │   ▸ 승인까지 5~10분 걸립니다     │
│                         │   │     조금 뒤 다시 눌러보세요      │
│   [ 다음 스텝 → ]        │   │   ▸ 등록 중 오류가 있었나요?     │
└─────────────────────────┘   │                                 │
                              │   [ 다시 확인 ]  [ AI에게 묻기 ] │
                              │   (3회 실패 시 수동 통과 요청)   │
                              └─────────────────────────────────┘
```

> 실패 문구가 **판정 근거를 말한다**는 게 핵심이다. "실패했습니다"만 있으면 사람은 막힌다.
> "0건으로 조회됩니다"는 다음 행동을 스스로 찾게 한다.

### 11-4. 그 외 화면

- `/my/academy/badges` — 뱃지장 (획득 조건·획득일·미획득은 실루엣)
- `/my/academy/[stepKey]` — 스텝 플레이어 (딥링크 가능해야 함. 알림에서 바로 진입)
- 대시보드 위젯 — 기존 `TutorialHubWidget` 확장 (다음 스텝 1개 + 스트릭)

---

## 12. 막혔을 때 — 3단 에스컬레이션

트레이너 없이의 진짜 승부처. **여기가 부실하면 자동 판정이 아무리 좋아도 이탈한다.**

```
1단  스텝별 트러블슈팅 (정적)
     · 스텝마다 troubleshoot[] 필수. 증상 → 원인 → 조치
     · 원천: src/lib/assistant/kb/troubleshooting.ts (536줄, 이미 있음)
     · 판정 실패 시 그 프로브에 해당하는 항목을 자동으로 펼쳐준다
        ↓ 해결 안 되면
2단  AI 상담 (스텝 컨텍스트 주입)
     · 기존 /api/assistant/chat 재사용
     · 프롬프트에 { 현재 스텝, 판정 실패 사유, 최근 프로브 응답 } 주입
     · ⚠️ 서버 LLM 키가 없으면 KB 검색 모드로 폴백 (지금 상태가 그렇다)
        ↓ 해결 안 되면
3단  사람 호출
     · 문의 티켓 생성 (스텝 키 + 판정 로그 자동 첨부 → 되묻기 없음)
     · ★ 이 비율이 지표다. 3단 도달률이 높은 스텝 = 콘텐츠가 부실한 스텝
```

> **3단을 없애지 않는다.** "완전 무인"을 목표로 3단을 막으면 막힌 사람은 그냥 떠난다.
> 목표는 3단 도달률을 **낮추는 것**이지 0으로 만드는 게 아니다.

---

## 13. 콘텐츠 제작 파이프라인

스텝 1개 만드는 작업 순서. **9칸을 다 채워야 발행된다.**

```
1. 소스 확보     · 전사본 구간 / 기존 guides.ts 스텝 / PDF 슬라이드
2. 화면 캡처     · 실제 화면 캡처 → capturedAt 기록 (민감정보 마스킹)
3. 핫스팟 좌표   · 관리자 화면에서 드래그 → {x,y,w,h,label,order}
4. 내레이션      · 전사본 구어체 → 문장 단위 정리 (한 문장 한 동작)
5. 판정 스펙     · L1 가능한가? → 프로브 지정 → pass() 작성
6. 트러블슈팅    · KB troubleshooting.ts 에서 관련 항목 연결 + 부족분 작성
7. 퀴즈          · L3인 경우만. 실제 사례 기반
8. TTS 생성      · 관리자 배치 실행 → mp3 + 타임스탬프
9. 리허설        · 신규 계정으로 처음부터 따라 하기 ← 이걸 건너뛰면 다 헛것
```

**콘텐츠 만료 관리**: `mockup.capturedAt` 이 6개월 지나면 관리자 화면에 재검토 플래그.
쿠팡 윙 UI는 바뀐다. 낡은 목업은 틀린 설명보다 나쁘다.

---

## 14. 구현 순서

| Phase | 내용 | 산출물 | 선행 |
|:--:|---|---|---|
| **0** | 진행 상태 서버 이전 | `academy_progress`/`xp_ledger` 마이그레이션, localStorage 마이그레이션 코드, `GET /api/academy/me` | — |
| **1** | **판정 엔진** | `src/lib/academy/verify/` + 프로브 8종 + `POST /verify` + 쿨다운/캐시/로그 | 0 |
| **2** | Act 1 (개업 8스텝) | 콘텐츠 8개 + 목업/핫스팟 + 지도 화면 + 스텝 플레이어 | 1 |
| **3** | Act 2 (등록 8스텝) | 원자료 영상 챕터 분할 + 콘텐츠 8개 | 2 |
| **4** | TTS | 배치 생성 스크립트 + `/api/academy/tts` + 플레이어 + 브라우저 폴백 | 3 |
| **5** | 게이미피케이션 | 레벨/뱃지/스트릭/퀘스트 + 뱃지장 + 아레나 부문 확장 | 1 |
| **6** | Act 3·4 (주문·CS) | 이벤트 트리거 연동 + 콘텐츠 14스텝 | 1 |
| **7** | Act 0·5 (진단·성장) | 진단 문항 + 시뮬레이터 + 성장 콘텐츠 | 6 |
| **8** | 에스컬레이션 고도화 | 스텝 컨텍스트 AI 상담 + 3단 도달률 대시보드 | 6 |

> **Phase 1이 전부다.** 판정 엔진 없이 콘텐츠부터 만들면 또 하나의 "안 읽는 문서"가 된다.
> Phase 0~1을 먼저 끝내고, 스텝 **한 개**(2-5 프리플라이트)를 끝까지 완성해서
> 실제로 판정이 도는 걸 본 다음에 나머지를 찍어낸다.

---

## 15. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **자동 판정 오판** | 신뢰 즉사 | 3회 실패 → 수동 통과 요청. 판정 근거를 화면에 항상 노출 |
| 쿠팡 API 호출량 | 레이트리밋 | 프로브 60초 캐시 + 스텝 20초 쿨다운 + 읽기 전용만 |
| 콘텐츠 노후화 | 틀린 설명 | `capturedAt` 6개월 만료 플래그 |
| 서버 LLM 키 없음 | AI 상담 반쪽 | TTS는 정적 파일, 상담은 KB 폴백. **키 의존 경로를 필수로 두지 않는다** |
| 증빙 이미지(사업자등록증) | 개인정보 | 비공개 버킷 + 서명 URL + 보관기간 명시 + 판정 후 삭제 옵션 |
| 게이미피케이션 과잉 | 유치함 | XP는 실제 달성에만. 읽기/클릭 보상 금지 |
| 영상 제작 부담 | 무한 지연 | `video` optional. 목업+TTS만으로 발행 가능 |
| 기존 트레이너 화면 파손 | 운영 중단 | `pt_education_progress` 단방향 미러링만. 기존 API 무수정 |

---

## 16. 확정된 결정 (2026-09-16)

### 16-1. 접근 범위 — **Act 0~2 무료 공개 · Act 3~5 PT 전용**

| Act | 공개 범위 | 이유 |
|---|---|---|
| Act 0 진단 | 🌐 로그인한 전원 | 유입 경로. "나한테 맞나"는 무료여야 한다 |
| Act 1 개업 | 🌐 로그인한 전원 | 입점까지는 막을 이유가 없다 |
| Act 2 등록 | 🌐 로그인한 전원 | 여기까지 해본 사람이 PT를 산다 |
| Act 3 주문 | 🔒 PT 학생 | 돈이 도는 구간부터가 코칭 가치 |
| Act 4 CS | 🔒 PT 학생 | 사고 대응 = 가장 비싼 지식 |
| Act 5 성장 | 🔒 PT 학생 | — |

구현: `AcademyStep.access: 'public' | 'pt'`. 무료 구간 끝(Act 2 완주) 지점에
**PT 전환 안내**를 둔다. 잠금 표시는 벌이 아니라 안내 문구로.

> 판정 엔진은 접근 범위와 무관하게 전원에게 돈다 — 자동 판정은 사람 손이 안 들어가므로
> 무료 구간을 열어도 운영 비용이 늘지 않는다. 늘어나는 건 쿠팡 API 호출뿐이고 그건 캐시로 막는다.

### 16-2. TTS — **GPT(OpenAI TTS)로 사전 생성 + 브라우저 폴백**

```
생성(오프라인 배치, 관리자 로컬에서 1회):
  narration[] → OpenAI TTS API → mp3 → Supabase Storage
  scripts/academy-tts-build.mjs  (OPENAI_API_KEY 는 로컬 .env.local 에서만 읽는다)

런타임(프로덕션):
  GET /api/academy/tts/[stepKey] → Storage 서명 URL 302
  파일 없으면 204 → 브라우저 speechSynthesis 폴백
```

> ★ **프로덕션에는 `OPENAI_API_KEY` 를 넣지 않는다.** 생성은 로컬 배치, 서빙은 정적 파일.
> 이렇게 하면 운영 중 키 유출·과금·장애 경로가 통째로 사라진다.
> 대본(`narration[]`)이 바뀌면 `script_hash` 가 달라져 자동으로 재생성 대상이 된다.

- 모델: `gpt-4o-mini-tts` (저렴·한국어 자연스러움). 목소리 1종으로 통일해 일관성 유지
- 문장별 mp3를 따로 뽑아 이어 붙인다 → **문장 하이라이트 타임스탬프를 정확히 얻는다**
  (한 덩어리로 뽑으면 어느 문장을 읽는 중인지 알 수 없다)

### 16-3. 구현 착수 — **Phase 0 + 1 먼저**

판정 엔진이 돌기 전에는 콘텐츠를 찍어내지 않는다.

### 16-4. 등록 경로 — **네이버 소싱 카탈로그로 단일화** (2026-09-16)

폴더 스캔(구글드라이브 + 도우미 앱) 경로를 초보 커리큘럼에서 뺐다.

| | 폴더 스캔 | 카탈로그 수동등록 |
|---|---|---|
| 구글드라이브 데스크톱 | 필요 | 불필요 |
| 도우미 앱 설치 | 필요 | 불필요 |
| 이미지 수급 | 내 PC 가 CDN 에서 직접 | 서버가 프록시로 대신 |
| 회사 PC·저사양 | 막히는 경우 있음 | 영향 없음 |

> 왕초보에게 프로그램 설치 두 개를 먼저 시키면 거기서 절반이 빠진다.
> 카탈로그는 설치가 0이다. 폴더·올인원 경로는 없애지 않았고, 아카데미가
> 기본으로 가르치지 않을 뿐이다.

그 결과 Act 1 에서 `구글드라이브 연동` / `도우미 설치` 두 스텝이 빠지고
`네이버 소싱 카탈로그 알아보기` 하나가 들어왔다.

### 16-5. L2 는 **번호 검증 우선**, 파일 증빙은 뒤로 (2026-09-16)

사업자등록이 파일 증빙(L2)이라 "준비 중" 으로 막혀 있었다 — **아카데미 첫 스텝이
통과 불가**였다. 파일·OCR·Storage 없이 검증되는 방법으로 먼저 열었다.

- 사업자등록번호: 국세청 체크섬 (오타·허위 번호를 걸러낸다)
- 통신판매업 신고번호: 형식 검사 (체크섬이 없다)
- ★ **과장 금지** — "국세청에 실제로 등록됐는지까지는 확인하지 않습니다" 를
  화면에 그대로 띄운다. 판정이 실제보다 세 보이면 그게 더 나쁘다.
- 번호 원본은 저장하지 않고 마스킹(`123-45-****`)만 남긴다. 테스트가 강제한다.

---

## 17. 진행 상황 (2026-09-23 기준)

| Phase | 내용 | 상태 |
|:--:|---|---|
| 0 | 진행 상태 서버 이전 (`academy_*` 6테이블, XP 원장) | ✅ 프로덕션 적용·제약 9항목 검증 |
| 1 | 판정 엔진 (프로브 8종, 러너, API 2개) | ✅ 배포 · **실사용 확인 완료** |
| 2 | 지도 + 스텝 플레이어 | ✅ 배포 · **실사용 확인 완료** |
| 3 | Act 0~5 콘텐츠 **33스텝** | ✅ 배포 |
| 4 | TTS — 브라우저 폴백(2순위) | ✅ 배포 / mp3 사전생성은 **안 하기로 결정**(2026-09-23) |
| 5 | 게이미피케이션 — XP·레벨·뱃지·스트릭·퀘스트 | ✅ 배포 |
| 6 | **공개 로드맵 `/start`** — Act 1·2 를 무료 구간으로 노출 | ✅ 배포 |
| 7 | 목업 + 핫스팟 (카탈로그) / 영상 챕터 5편 | 🔶 카탈로그만 · 윙 화면 없음 |

**스텝 분포**: Act0 3 · Act1 7 · Act2 8 · Act3 6 · Act4 5 · Act5 4 = **33**
**판정 분포**: L1 자동 9 · L2 번호 2 · L3 퀴즈 22 · 합계 2,710 XP

### 17-1. 실사용 확인 (2026-09-23, 프로덕션 브라우저 세션)

미들웨어가 `/api/*` 를 전부 401 로 막아 밖에서는 확인이 안 됐던 부분을 로그인 상태로 확인했다.

- 환영 화면 → 첫 단계 자동 진입 ✅
- 스텝 플레이어(목표·이유·내레이션·할 일·트러블슈팅·판정) ✅
- **L1 자동 판정이 실제로 쿠팡에 붙는다** — `act1-05-api` 에서 `쿠팡 API 연결 실패: API 401:
  Hmac key is expired.` 를 돌려줬다. 실패 사유를 그대로 말해주는 설계가 의도대로 동작한다.
- L2 번호 입력 화면과 "국세청에 실제로 등록됐는지까지는 확인하지 않습니다" 고지 ✅
- `/api/academy/quests` · 스트릭 · 33스텝 배포 ✅

### 17-2. 공개 로드맵 `/start` (2026-09-23 신규)

Act 1·2 = **사업자등록 → 상품 업로드 → 승인 확인 15단계**. 로그인 없이 전부 읽을 수 있고,
각 단계 끝에서 아카데미(자동 판정)로 들어간다.

- 콘텐츠 원천은 아카데미 단계 데이터 하나다. 예전 `/start` 는 손으로 적은 5단계라
  아카데미와 답이 갈려 있었다(업종코드 47911 vs 525101, 홈택스 vs 토스).
- **`/start` 는 서버 컴포넌트다.** 클라이언트에서 아카데미를 직접 import 하면
  퀴즈 정답과 PT 전용(Act 3~5) 콘텐츠가 공개 번들로 나간다. 빌드 산출물로 확인했다.
- 따라하기 체크리스트는 `AcademyStep.howto` — `narration`(귀로 듣는 대본)과 문장이 다르다.

### 17-3. 사업자등록·통신판매업은 토스로 (2026-09-23 확정)

홈택스·정부24 기준이던 두 단계를 **토스페이먼츠 바로신청** 기준으로 다시 썼다.

- 공동인증서·증빙서류·대행 수수료가 없고, 두 신고를 한 자리에서 이어서 한다
- 통신판매업의 필수 첨부물인 **구매안전서비스 이용 확인증이 같이 처리된다** — 은행에 갈 일이 없다
- ★ **등록면허세(4~6만원)는 면제가 아니다.** 무료인 건 대행 수수료다. 이걸 흐리면
  "무료라더니" 가 첫 단계에서 터진다. 화면에 그대로 적었다
- 토스 바로신청은 통신판매업 준비 중인 **개인사업자 간이신청**만 된다. 법인·다른 업종은 홈택스

### 남은 일 (우선순위)

1. **쿠팡 API 키 재발급** — 지금 만료돼 있다(`Hmac key is expired`). 이것 때문에
   `act1-05`·`act1-06` 판정이 통과 불가이고, 대량 등록 화면의 출고지·반품지 자리에
   401 오류가 떠서 **Act 2 목업을 정상 상태로 캡처할 수 없다.** 다른 모든 것의 앞이다.
2. **쿠팡 윙 화면 목업** — 아카데미에서 가장 많이 막히는 구간(API 키 발급·IP 10개 입력)에
   그림이 없다. 윙 로그인이 필요해 보류 중.
3. **대량 등록 화면 목업** — 1번이 풀리면 바로 찍을 수 있다(전체 검증 버튼, 등록 제외 아이콘).
4. L2 파일 증빙(Storage + 판정 라우트) — 번호 검증으로 우회했으므로 급하지 않다.

### 영상 챕터 — 자체 호스팅 (2026-09-23)

원본 `등록자료.mp4` 는 **구글 Meet 화면공유 녹화**다. 화면 안에 또 브라우저가 들어 있어
북마크바·탭 제목·작업표시줄에 수강생의 개인 흔적이 그대로 남는다. 설계도 초안이
"YouTube 비공개 업로드" 로 적어둔 건 이 사실을 모르고 쓴 것이다.

- `ffmpeg -vf "crop=1312:450:14:150"` 로 안쪽 앱 영역만 남기고, 전사본 타임코드로 5구간 분할
- Act2: 프리플라이트 / 옵션값 / 대표이미지 / 상세페이지 / 등록 제외
- 버킷 `academy-video` 는 **비공개**. `/api/academy/video/[stepKey]` 가 1시간 서명 URL 을 준다
- ⚠️ 영상 속 앱 UI 는 현재보다 구버전이다. `capturedAt` 만료 관리 대상

### 아직 결정하지 않은 것

- **TTS mp3**: **안 만들기로 결정(2026-09-23)** — 브라우저 speechSynthesis 폴백으로 간다.
  파이프라인(`scripts/academy-tts-build.mjs`)은 남겨두므로 마음이 바뀌면 키만 넣으면 된다.
- **Act 1·3·4 신규 촬영**: 보류 (`video` optional 이므로 목업만으로 발행 가능)
- **L2 자동 조회**: 국세청 사업자 상태조회 / 공정위 통신판매사업자 조회 API 연동 여부.
  1차는 형식 검증 + 이미지 보관까지만
- **3단(사람 호출)**: **유지**. 도달률을 콘텐츠 품질 지표로 관리
- **과세 유형 권고가 문서마다 다르다**: `guides.ts` 는 "쿠팡 셀러는 일반과세자여야" 라고
  쓰고, 옛 `/start` 는 "간이과세자 추천" 이었다. 아카데미에는 한쪽으로 단정하지 않고
  "위탁판매는 매입세액 공제 때문에 일반과세자가 유리한 경우가 많다 / 나중에 바꿀 수 있다"
  로 적어뒀다. **어느 쪽이 회사의 공식 권고인지 정하고 세 군데를 맞춰야 한다.**

---

*작성 2026-09-16 · 근거: 레포 실제 코드 조사 + `G:\내 드라이브\landingpage\coupanglanding\자료\` 원자료*
