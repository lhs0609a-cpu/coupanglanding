/**
 * 올인원 생성 프롬프트 빌더 (한국어 이커머스)
 * ---------------------------------------------------------------------------
 * 필드: 노출상품명/제목 · 카테고리 · 상세페이지(스토리).
 * - 페르소나 시드로 셀러마다 톤을 다르게 → 아이템위너(동일문구) 회피.
 * - 금지어/효능과장 회피를 시스템 프롬프트로 1차 차단(+ 사후 compliance-mini 검사).
 */

const FORBIDDEN_RULE =
  '절대 금지: 질병 치료/예방/완화 표현(치료·완치·항암·당뇨·면역력 증진 등), 의약품 오인, ' +
  '화장품 의학적 효능(미백·주름개선·재생·안티에이징 등 기능성 표현), ' +
  '"100% 효과/보장", "최고·1위·유일·최초" 같은 객관적 근거 없는 최상급/절대 표현, 부작용 없음. ' +
  '또한 존재하지 않는 인증·시험·임상·특허를 지어내지 말 것(예: "FDA 인증", "임상시험 완료"). ' +
  '주어진 정보에 없는 수치는 절대 만들지 말 것 — 함량 퍼센트(%), 일일권장량 대비 %, "○○% 함유/달성" 같은 수치는 입력에 명시된 경우에만 쓰고, 없으면 수치 없이 표현할 것. ' +
  // 셀러 현장 제보(2026-07-29): 아래 4단어는 광고법 위반으로 누적 집계되어 계정 정지 사례가 있다.
  //   원산지는 고시정보·속성으로 등록되므로 상품명·본문에 쓸 이유가 없다.
  '다음 단어는 어떤 경우에도 쓰지 말 것(광고법 위반 누적 → 계정 정지): "유기농", "국산", "국내산", "포도당", "수액". ' +
  '원산지를 말하고 싶어도 상품명·본문에는 넣지 말 것(원산지는 별도 고시항목으로 등록된다). ' +
  '효능을 단정하지 말고 제품 특징·성분·사용감·편의 중심으로 표현할 것. 한자(漢字) 금지, 순한국어만.';

export const PERSONAS = [
  { key: '효능정보', style: '성분과 스펙을 신뢰감 있게 설명하는 전문가 톤', focus: '성분, 함량, 규격, 사용법' },
  { key: '가성비',   style: '대용량·실용성을 강조하는 알뜰 톤',           focus: '용량, 구성, 경제성, 활용도' },
  { key: '프리미엄', style: '품질과 고급스러움을 강조하는 럭셔리 톤',     focus: '소재, 마감, 디테일, 브랜드감' },
  { key: '감성',     style: '일상 장면을 그리는 따뜻한 감성 톤',         focus: '사용 순간, 분위기, 만족감' },
  { key: '실용후기', style: '실사용자가 알려주듯 솔직 담백한 톤',         focus: '편의, 장점, 사용 팁' },
];

function hashSeed(s) {
  let h = 2166136261;
  for (const ch of String(s || 'default')) { h ^= ch.charCodeAt(0); h = Math.imul(h, 16777619); }
  return Math.abs(h);
}
export function pickPersona(seed) { return PERSONAS[hashSeed(seed) % PERSONAS.length]; }

/** 노출상품명/제목 — JSON {displayName, keywords}. fixNote: 재생성 시 직전 문제 교정지시. */
export function buildTitlePrompt(p, persona, { fixNote = '' } = {}) {
  // 쿠팡 상위노출 상품명 = 구매자가 검색창에 실제로 치는 명사·스펙의 나열.
  //   구조: [브랜드] [제품명] [핵심스펙(용량·수량·규격)] [속성키워드]
  //   ⚠️ 노출명은 카피가 아니다 — 페르소나 톤/형용사를 넣으면 검색 매칭이 깨진다.
  const system = `당신은 쿠팡 검색 상위노출(SEO) 상품명 생성기다. 광고 카피가 아니라, 구매자가 검색창에 실제로 입력하는 "명사 + 스펙 + 다양한 검색 속성어" 조합을 만든다.
${FORBIDDEN_RULE}

[출력 구조] 상품명을 **코어와 속성어로 나눠서** 준다(붙이는 건 시스템이 한다):
  core  = 제품 핵심명 + 핵심스펙(용량/수량/규격/맛/종류/재질). 여기까지가 상품의 정체다.
  attrs = 검색 속성어 8개(용도·대상·형태·특성 등). 이 중 5개가 상품명 뒤에 붙는다.
[규칙]
- ⚠️ 브랜드명은 절대 넣지 말 것. 브랜드는 별도 필드로 등록되므로 노출명에서는 뺀다(예: "현대농산 발아현미"→"발아현미", "에코서트 알로에"→"알로에"). 상호·상표·브랜드로 보이는 고유명(회사명·스토어명)은 제외한다.
- ⚠️ 길이를 억지로 늘리지 말 것. 쿠팡은 "메인 키워드 + 속성어"의 간결한 상품명을 권한다 — 키워드를 길게 나열하면 스터핑으로 역효과다. core 는 25자 안팎이면 충분하다.
- 원본 상품명에 있는 실제 스펙(용량 750ml, 수량 24개, 규격 등)은 반드시 보존한다(지어내지 말 것).
- ⚠️ 단, 원산지 표기(국산·국내산·유기농 등)는 원본에 있어도 **빼고** 쓴다 — 광고법 위반 누적 대상이라 별도 고시항목으로만 등록한다.
- attrs 는 서로 다른 각도로 8개: 용도(요리용·선물용·간식용), 대상(성인·아이·반려), 형태(대용량·개별포장·낱개), 특성·동의어(다진마늘=깐마늘) 등. 앞쪽일수록 중요한 것으로 정렬한다.
- ⚠️ 같은 단어를 두 번 이상 반복하지 말 것 — "혼합곡 기타곡류 혼합곡 기타곡류 요리용 요리용" 같은 도배 금지. 각 단어는 딱 한 번만.
- 절대 넣지 말 것(검색 노이즈·과장): 합리적/최고/프리미엄/인기/추천/특가/득템/강추/알뜰/필수템 같은 주관·홍보 형용사, "세트/선물세트"(원본에 실제로 세트 구성일 때만), 느낌표·이모지·특수기호.
- ⚠️ 문장이 아니라 명사구다. 서술어·조사·부사를 절대 쓰지 말 것: "입니다·됩니다·드셔도·재배한·정성스럽게·안심하고·좋은·완벽한·~에서" 등이 들어가면 실패다. 오직 명사(제품명·스펙·검색어)만 띄어쓰기로 나열한다.
- 원본 상품명이 설명 문장(예: "…에서 정성스럽게 재배한 …입니다")이어도 그대로 쓰지 말고, 거기서 핵심 명사(제품·스펙)만 뽑아 재구성한다.

출력은 JSON만: {"core": "...", "attrs": ["...8개..."], "keywords": ["...8개..."]}.
 · core    = 제품명 + 핵심스펙 (브랜드 제외)
 · attrs   = 상품명 뒤에 붙일 속성어 8개 (시스템이 5개를 골라 붙인다)
 · keywords = 상품명에 안 들어갈 **검색어 태그**용 8개. 쿠팡은 검색어를 20개까지 따로 받는다.
   ⚠️ core·attrs 와 겹치지 않게, 동의어·다른 표기·구체적 용도로 채운다(겹치면 태그가 버려진다).`;
  const prompt = `원본 상품명: ${p.originalName}
카테고리: ${p.categoryPath || '미상'}
브랜드(노출명에 넣지 말 것): ${p.brand || '없음'}
핵심 특징(스펙): ${(p.features || []).join(', ') || '미상'}

[좋은 예 — 브랜드 제외]
- 원본 "리무스 모스카토 무알콜 스파클링 와인 750ml 달콤한"
  → {"core":"모스카토 무알콜 스파클링 와인 750ml","attrs":["논알콜","달달한","파티","선물용","홈파티","무알콜와인","스파클링","디저트와인"],"keywords":["논알콜와인","모스카토와인","달달한와인","파티용와인","무알콜스파클링","와인선물","홈파티와인","저도수와인"]}
- 원본 "곰곰 국내산 깐마늘 1kg 아삭한 요리용"  ← 브랜드와 "국내산"을 함께 뺀다
  → {"core":"깐마늘 1kg","attrs":["다진마늘","요리용","김장","반찬","대용량","아삭한","국거리","양념용"],"keywords":["깐마늘1kg","다진마늘","김장마늘","요리용마늘","마늘대용량","깐마늘업소용","마늘반찬","생마늘"]}
[나쁜 예(문장 원본) → 이렇게 명사구로 고쳐라]
- 원본 "에서 정성스럽게 재배한 기능성 쌀 입니다 안심하고 드셔도 됩니다 혼합곡 18곡 4kg"
  → (X) core 에 "에서 정성스럽게 재배한 … 입니다" 를 그대로 담는 것
  → (O) {"core":"기능성 쌀 혼합곡 18곡 4kg","attrs":["잡곡밥","건강잡곡","대용량","요리용","반찬용","혼합잡곡","현미밥","다이어트"],"keywords":["18곡잡곡","혼합곡4kg","잡곡밥쌀","기능성쌀","건강잡곡밥","대용량잡곡","혼합곡대용량","잡곡세트"]}
→ 위 상품의 core(제품명+스펙), attrs 8개, keywords 8개를 JSON으로. 브랜드명 없이 명사만, 문장·반복 금지.${fixNote ? `\n\n[직전 출력 문제 — 반드시 교정] ${fixNote}` : ''}`;
  return { system, prompt, format: 'json', options: { temperature: fixNote ? 0.25 : 0.4, num_predict: 260 } };
}

/** 카테고리 — 후보가 있으면 그 중 선택, 없으면 경로 추론. JSON {categoryPath, confidence} */
export function buildCategoryPrompt(p, candidates = []) {
  const hasCand = candidates.length > 0;
  // 소싱 원본 카테고리(네이버 등)는 이미 사람이 분류한 near-ground-truth → 앵커로 준다.
  const srcCat = String(p.categoryPath || '').trim();
  const system = `당신은 쿠팡 카테고리 분류기다. 상품명 글자만 보지 말고 "원본 카테고리·브랜드·특징"으로 상품이 실제로 무엇인지 이해한 뒤 고른다.
⚠️ 핵심 규칙: **소싱 원본 카테고리와 의미가 일치하는 후보**를 고른다. 원본이 '맥주/음료'면 절대 '도서/가구/완구'를 고르지 않는다(상품명에 우연히 든 브랜드어 '클라우드'·'허브'·'와인' 등에 속지 말 것 — 그건 책·가구가 아니다).
출력은 JSON만: {"categoryPath": "대>중>소>세부", "confidence": 0~1}.`;
  const ctxLines = [
    `상품명: ${p.originalName}`,
    srcCat ? `원본 카테고리(소싱처 분류 — 이것과 의미가 맞는 후보를 최우선): ${srcCat}` : '',
    p.brand ? `브랜드: ${p.brand}` : '',
    `특징: ${(p.features || []).join(', ') || '미상'}`,
  ].filter(Boolean).join('\n');
  const prompt = hasCand
    ? `${ctxLines}\n\n후보 카테고리(아래 문자열 중 하나를 글자 그대로 복사):\n${candidates.map((c, i) => `${i + 1}. ${c}`).join('\n')}\n→ 원본 카테고리와 의미가 가장 일치하는 1개의 categoryPath 를 후보 문자열 그대로(변형·한자추가 금지) JSON으로. 확신이 낮으면 confidence 를 낮게.`
    : `${ctxLines}\n→ 쿠팡식 카테고리 경로(대>중>소>세부)를 추론해 JSON으로. 한자 금지, 순한국어.`;
  return { system, prompt, format: 'json', options: { temperature: 0.2, num_predict: 120 } };
}

/** 옵션 — JSON {options:[{name,value,unit?}]}. 상품명/특징에서 도출 가능한 것만(환각 금지) */
export function buildOptionsPrompt(p) {
  const system = `당신은 쿠팡 상품 옵션 추출기다. 출력은 JSON만: {"options":[{"name":"옵션명","value":"옵션값","unit":"단위(없으면 생략)"}]}.
규칙: 상품명/특징에 실제로 드러난 정보(용량·수량·색상·사이즈·맛/종류 등)만 옵션으로. 없는 스펙은 절대 지어내지 말 것. 1~4개. 한자 금지.`;
  const prompt = `상품명: ${p.originalName}
특징: ${(p.features || []).join(', ') || '없음'}
→ 위에서 확인되는 구매옵션만 JSON으로. (예: 용량 50ml, 수량 120정, 색상 베이지)`;
  return { system, prompt, format: 'json', options: { temperature: 0.1, num_predict: 200 } };
}

/**
 * 상품 종류(kind) 판별 — 후기의 감각 묘사가 카테고리에 맞아야 한다.
 *   ⚠️ 예전엔 프롬프트 감각 예시·문체 few-shot 이 전부 "과일"이라, 발아현미(곡물)에
 *      "과즙이 손등까지", "베어 물면 아삭" 같은 과일 표현이 그대로 복제됐다(실측 환각).
 *   → 종류별로 "묘사할 감각"과 "쓰면 안 되는 감각"을 다르게 준다.
 */
export function categoryKind(categoryPath = '', leaf = '') {
  const s = `${leaf} ${categoryPath}`;
  if (/과일|청과|사과|배\b|귤|감\b|포도|딸기|수박|참외|복숭아|자두|체리|블루베리|망고|키위|오렌지|자몽|멜론|앵두|무화과/.test(s)) return 'fruit';
  if (/식품|음료|과자|간식|커피|차류|곡|쌀|잡곡|현미|견과|건어물|수산|정육|축산|소스|양념|장류|반찬|김치|면류|라면|즉석|베이커리|빵|우유|유제품|치즈|꿀|잼|분말|건강식품|영양제/.test(s)) return 'food';
  if (/화장품|스킨|토너|로션|크림|세럼|앰플|에센스|마스크팩|클렌징|바디|헤어|샴푸|린스|선크림|자외선|뷰티|향수|립|쿠션|파운데이션/.test(s)) return 'beauty';
  if (/가전|전자|디지털|컴퓨터|노트북|모니터|키보드|마우스|이어폰|헤드폰|스피커|충전|배터리|보조배터리|카메라|TV|텔레비전|청소기|공기청정|드라이어|면도기|선풍기|가습기|제습기/.test(s)) return 'electronics';
  if (/의류|패션|옷|티셔츠|셔츠|바지|청바지|원피스|자켓|코트|니트|스웨터|신발|운동화|구두|가방|지갑|모자|양말|속옷|레깅스|점퍼|패딩/.test(s)) return 'fashion';
  if (/가구|침대|매트리스|소파|책상|의자|선반|수납|옷장|주방|그릇|냄비|프라이팬|텀블러|침구|이불|베개|커튼|생활|욕실|청소|세제|수건|매트/.test(s)) return 'home';
  if (/유아|출산|기저귀|물티슈|분유|이유식|아기|완구|장난감|육아/.test(s)) return 'baby';
  if (/반려|강아지|고양이|사료|간식|펫|애견|고양이모래/.test(s)) return 'pet';
  return 'generic';
}

/** 종류별 감각 묘사 가이드 + 문체 few-shot. sensory=묘사할 것, forbid=쓰면 안 될 감각, shot=결 참고. */
const KIND_GUIDE = {
  fruit: {
    sensory: '한 입 베어 물 때의 아삭함·과즙, 달큰한 향, 손질/보관의 편함',
    forbid: '',
    shot: '"택배 열자마자 향이 훅 올라오더라구요~ 한 입 베어 무니까 사각 소리 나면서 과즙이 손등까지 흘러서 애들이 더 좋아했어요. 예전에 마트에서 샀다가 이틀 만에 물러버린 적 있어서 걱정했는데, 냉장고에 두고 하나씩 꺼내 먹어도 마지막까지 아삭하더라구요."',
  },
  food: {
    sensory: '포장을 열 때 올라오는 냄새, 씹거나 조리했을 때의 식감·풍미, 보관/양의 실감',
    forbid: '과즙·과육·포도·베어 물면 같은 생과일 표현(먹더라도 과일이 아니면 쓰지 말 것)',
    shot: '"봉지 뜯자마자 고소한 냄새가 확 올라오더라구요~ 한 줌 씹어보니 알이 실하고 씹는 맛이 좋아서 놀랐어요. 양이 많아 부담될까 했는데 소분해 두니까 아침마다 한 컵씩 꺼내 쓰기 딱 좋더라구요. 애들 밥에 섞어주기도 편해서 재구매 각이에요~"',
  },
  beauty: {
    sensory: '제형(젤/크림/오일)의 질감, 바를 때의 발림성과 흡수, 향, 바른 뒤 피부 느낌',
    forbid: '맛·식감·아삭·과즙 같은 먹는 상품 표현(피부에 바르는 제품이다)',
    shot: '"펌핑하니 묽지도 되지도 않은 젤이 나왔어요. 얼굴에 펴 바르니 끈적임 없이 금방 스며들고, 향은 거의 없어서 아침저녁 부담 없이 썼습니다. 건조하던 볼도 당김이 덜하더라고요."',
  },
  electronics: {
    sensory: '손에 잡히는 무게감·마감, 버튼/조작감, 소음·정숙함, 발열, 연결/작동 속도',
    forbid: '맛·식감·향·과즙 같은 먹는 상품 표현(전자제품이다)',
    shot: '"손에 쥐어보니 생각보다 묵직하고 마감이 깔끔했어요. 버튼을 눌러보니 딸깍하는 감이 좋고, 작동 소리도 조용한 편이라 밤에 써도 거슬리지 않았습니다. 연결도 한 번에 잡혔어요."',
  },
  fashion: {
    sensory: '원단의 촉감·두께, 입었을 때의 핏·신축성, 색감, 활동성',
    forbid: '맛·식감·과즙 같은 먹는 상품 표현(입는 옷/잡화다)',
    shot: '"택배를 열자마자 원단부터 만져봤는데 도톰하면서 부드러웠어요. 입어보니 어깨선이 딱 맞고 팔을 올려도 당기지 않더라고요. 화면 색보다 살짝 차분한 톤이라 오히려 마음에 들었습니다."',
  },
  home: {
    sensory: '재질의 촉감·무게, 마감, 크기감, 실제로 써봤을 때의 편의',
    forbid: '맛·식감·과즙 같은 먹는 상품 표현(생활/주방 용품이다)',
    shot: '"상자에서 꺼내니 생각보다 묵직하고 마감이 매끈했어요. 실제로 올려놓고 써보니 크기가 딱 맞아 자리를 안 차지하고, 손질도 물로 헹구면 끝이라 편했습니다."',
  },
  baby: {
    sensory: '촉감(부드러움), 냄새(무향/순함), 아이에게 썼을 때의 자극 없음·편의',
    forbid: '맛·과즙 같은 표현(이유식이 아니면 쓰지 말 것)',
    shot: '"한 장 꺼내보니 도톰하고 부드러웠어요. 향이 거의 없어서 아이 얼굴에도 부담 없이 썼고, 물기도 촉촉하게 오래 남아 있어 몇 장이면 충분했습니다."',
  },
  pet: {
    sensory: '냄새·알갱이 크기/식감(사료), 아이(반려동물)의 반응, 급여/보관 편의',
    forbid: '사람이 먹는 것처럼 쓰지 말 것(반려동물용이다)',
    shot: '"봉지를 여니 잡내 없이 구수한 냄새가 났어요. 알갱이가 작아 우리 아이가 남기지 않고 잘 먹더라고요. 지퍼백이라 보관도 편했습니다."',
  },
  generic: {
    sensory: '첫인상, 손에 잡히는 느낌·무게·마감, 실제로 써봤을 때 달라진 점',
    forbid: '상품 종류에 맞지 않는 감각(먹는 것이 아니면 맛·식감·과즙 금지)',
    shot: '"택배 열어서 처음 만져봤을 때 마감이 생각보다 꼼꼼해서 놀랐어요. 며칠 써보니까 손에 익어서 이제 없으면 아쉬울 것 같더라구요~ 처음엔 반신반의했는데 이 값이면 충분히 값어치 해요."',
  },
};

/**
 * 쿠팡 카테고리 leaf 라벨 → 본문에서 쓸 "상품 이름"으로 정규화.
 * ---------------------------------------------------------------------------
 * ⚠️ 쿠팡 인덱스 leaf 의 18%(16,259개 중 2,982개)가 '혼합곡/기타곡류',
 *    '락커/캐비닛/사물함' 처럼 **슬래시로 여러 이름을 이어붙인 분류 라벨**이다.
 *    이걸 그대로 "상품 이름"으로 주고 SEO 검증까지 "본문에 2회 이상"으로 강제하면
 *    자연스러운 한국어 문장에 넣는 것이 구조적으로 불가능하다 → 매 시도 검증 실패 →
 *    교정지시("이 문자열을 2회 넣어라")가 다음 프롬프트에 주입되고, 모델이 굴복해서
 *    "후기처럼 혼합곡/기타곡류" 같은 **라벨 제목줄**을 본문 맨 앞에 박았다(실측).
 * → 본문 지칭어는 대표 토큰 하나(display)만 쓰고, SEO 검증은 variants 중 아무거나 인정한다.
 * @returns {{display:string, variants:string[], isMulti:boolean}}
 */
export function leafForms(leaf) {
  const raw = String(leaf || '').trim();
  if (!raw) return { display: '', variants: [], isMulti: false };
  const parts = raw.split(/[/·,]/).map((s) => s.trim()).filter(Boolean);
  if (parts.length <= 1) return { display: raw, variants: [raw], isMulti: false };
  // '기타OO' 는 분류용 접두사일 뿐 상품 이름이 아니다 → 알맹이만("기타곡류"→"곡류").
  const cleaned = parts.map((s) => s.replace(/^기타\s*/, '').trim()).filter((s) => s.length >= 2);
  const display = cleaned[0] || parts.find((s) => s.length >= 2) || parts[0];
  return { display, variants: [...new Set([...parts, ...cleaned, display])], isMulti: true };
}

/** 상세페이지(스토리) — 섹션형 한국어 카피. 일반 텍스트(마크다운 허용).
 *  p: { originalName, categoryPath, features[], leaf?, seoKeywords?[] }
 *  fixNote: 재시도 시 직전 출력의 문제를 교정 지시로 주입(검증 실패 피드백). */
export function buildDetailPrompt(p, persona, { maxTokens = 1100, fixNote = '' } = {}) {
  const rawLeaf = (p.leaf || (p.categoryPath || '').split('>').pop() || p.originalName || '').trim();
  // 본문에서 부를 이름은 항상 단일 명사 — 슬래시 라벨을 그대로 부르게 두면 안 된다(leafForms 주석).
  const { display: leaf, isMulti } = leafForms(rawLeaf);
  const seo = (p.seoKeywords || []).filter(Boolean).slice(0, 8);
  const kind = categoryKind(p.categoryPath, rawLeaf); // 종류 판별은 라벨 원문이 정보가 더 많다
  const guide = KIND_GUIDE[kind] || KIND_GUIDE.generic;
  const system = `당신은 쿠팡 1페이지 상위노출과 구매전환을 동시에 잡는 한국어 상세페이지 카피라이터다. ${persona.style}.

[작성 대상] 오직 "${leaf}" 상품 하나만 다룬다(분류: ${p.categoryPath || leaf} — ⚠️ 이 분류 문자열 자체를 본문에 쓰지 말 것. "식품 신선식품 과일류" 같은 카테고리 경로를 문장에 넣으면 비문이 된다. 본문에서는 "${leaf}" 라는 상품 이름만 쓴다). 다른 종류의 상품이나 다른 카테고리의 기능·부품·효능(예: 시계인데 매트리스·수면, 마우스인데 공기청정·난방)을 절대 언급하지 않는다. 모든 문장이 "${leaf}"에 정확히 들어맞아야 한다.${isMulti ? `\n⚠️ "${rawLeaf}" 처럼 슬래시(/)로 여러 이름이 붙은 분류 라벨은 사람이 쓰는 말이 아니다 — 본문에 절대 그대로 쓰지 말고, 언제나 "${leaf}" 라는 이름으로만 불러라.` : ''}

[이 상품만의 특징 — 매우 중요] 카테고리 일반론("그냥 바디로션이라 촉촉해요")에 머물지 마라. 상품명 "${p.originalName}"과 특징에 담긴 **이 상품만의 차별점**(핵심 성분·인증·함량/농도·용량·색상·향·기능 키워드 등, 예: "알로에 96%", "세라마이드", "에코서트 인증", "무알콜", "500ml 대용량")을 찾아, 본문에서 구체적으로 이름을 불러 이야기한다. 같은 카테고리의 다른 제품과 무엇이 다른지가 드러나야 한다. (단, 상품명·특징에 근거 없는 성분·효능·인증을 지어내지는 말 것.)

[톤 — 가장 중요] 이건 광고 문구가 아니라 **실제 구매자(특히 아이 키우는 엄마)가 카페·블로그에 남긴 후기 글**이다. 받아보고 써본 사람이 친구에게 수다 떨듯 쓴다.
  · ⚠️ 친근한 "해요체" 엄마 말투로 쓴다: "받아보니 너무 좋더라구요~", "우리 애가 잘 먹어요", "생각보다 알차서 놀랐어요", "재구매 각이에요" 처럼. 문장 끝을 "~했어요/~더라구요/~좋아요/~같아요"로 맺는다. 딱딱한 "~했다/~이다/~하였다" 문어체는 절대 금지(후기가 아니라 설명문처럼 읽힌다).
  · 카탈로그 문체 금지: "제철의 신선함을 그대로", "고급 과일의 맛과 품질" 같이 명사로 끝나는 라벨식 표현은 후기가 아니다.
  · 대신 장면과 감각으로 쓴다 — 택배 상자를 열었을 때, 처음 써봤을 때, 며칠 써보니 어땠는지.
  · ⚠️ 이 상품은 "${leaf}"(분류: ${kind})다. 묘사할 감각은 이런 것들이다: ${guide.sensory}. 이 중 최소 두 가지를 구체적으로 쓴다.${guide.forbid ? ` 절대 쓰지 말 것: ${guide.forbid}.` : ''} 상품 종류에 맞지 않는 감각(예: 먹는 것이 아닌데 "맛있다·아삭·과즙", 바르는 것이 아닌데 "발림성")을 지어내면 명백한 오류다.
  · 진짜 후기처럼: 완벽하게 좋다고만 하지 말고, 처음의 의심이나 살짝 아쉬웠던 점 한 가지쯤은 솔직하게 적어도 좋다(오히려 더 믿음이 간다). 광고처럼 "지금 바로 주문하세요"로 몰아붙이지 않는다.
  · 특정인을 사칭한 거짓 후기("제가 3개월 써보니", "구매자 김OO님")는 쓰지 않는다. 누구나 겪는 장면 + 솔직한 추천 톤으로.

[흐름 — 후기 글의 뼈대(PAS). 이 순서를 반드시 지킨다]
⚠️ 아래 1)~7)은 **너에게 주는 설계도일 뿐**이다. "후킹", "문제 증폭", "해결", "핵심 장점",
   "망설임 해소", "추천 대상", "마무리", "감각 묘사" 같은 단계 이름을 본문에 절대 쓰지 마라
   (실제로 자주 저지르는 실수다). 소제목이 필요하면 내용에 맞는 자연스러운 문장으로 쓴다.
   글 끝에 키워드 목록이나 구분선(---) 같은 부록도 붙이지 마라 — 본문만 출력한다.
1) 후킹 한 문장: **구매자가 실제로 겪는 실패·불편·불안**을 콕 집는다. 칭찬으로 시작하지 말 것.
   (나쁜 예: "배 한 조각이 얼마나 큰 행복인지 아시나요?" ← 고민이 아님)
   (좋은 예: "배 한 박스 시켰다가 물러서 절반을 버린 적, 다들 있으시죠?")
2) 문제 증폭 한두 문장: 그 상황이 왜 손해인지(돈·시간·기분) 짧게 찌른다.
3) 해결 — "${leaf}"를 만나고 뭐가 달라졌는지. 장면 + 감각 묘사로 풀어낸다(문단 2~3개).
4) 핵심 장점 '- ' 불릿 3~4개. ⚠️ "라벨: 설명" 형태 금지. 각 불릿은 **완결된 문장**으로,
   스펙이 아니라 "그래서 내 생활이 어떻게 편해지는지"를 쓴다.
   (나쁜 예: "- 다양한 활용법: 샐러드, 디저트에 활용 가능합니다.")
   (좋은 예: "- 15kg를 시켜도 무르지 않아서, 한 달 내내 아침마다 하나씩 깎아 먹었습니다.")
5) 망설임 하나를 정면으로 짚고 답한다(가격이 부담될까 / 양이 많을까 / 보관이 어려울까 등).
6) 어떤 사람·어떤 순간에 특히 좋은지 1~2문장.
7) 마무리 한 문장 — 담백하게 권한다. 느낌표 남발·과장 금지.

[상투구 금지 — 하나라도 쓰면 글이 광고로 읽힌다]
"풍성한 식탁", "행복을 더하다", "완벽한 선택", "소중한 선택", "탁월한 선택", "손색이 없",
"활력을 불어넣", "후회하지 않으실", "정성스러운 선물", "만족도를 높여", "그 이상의 가치",
"당신의 일상을", "지금 바로 만나보세요" 류의 관용 광고문구를 쓰지 말 것.

[쿠팡 SEO]
- 분량: 공백 제외 약 600~1200자(너무 짧으면 노출·신뢰 약함, 너무 길면 이탈).
- 키워드: 다음을 본문에 자연스럽게 녹인다(어색한 나열 금지): ${seo.join(', ') || leaf}. 특히 "${leaf}"는 본문 전체에서 2~4회 자연스럽게 반복 노출.

[문체] 순한국어 존댓말. 한자(漢字)·중국어·일본어·영어 문장 금지(USB·ml·LED 같은 표준 단위/약어만 허용). 같은 문장·문구 반복 금지.

[금지] ${FORBIDDEN_RULE}

[출력] 완성된 카피 본문만. '헤드라인','불릿','추천 대상','(1)','상세페이지 본문' 같은 지시문·라벨·번호를 출력에 쓰지 않는다.
⚠️ 마크다운 강조기호를 절대 쓰지 말 것 — **볼드**, __밑줄__, ##제목, *기울임* 금지. 별표(*)는 어떤 용도로도 쓰지 않는다(불릿은 '- ' 만 사용). 강조는 문장 자체의 힘으로 한다.${fixNote ? `\n\n[직전 출력 문제 — 반드시 교정] ${fixNote}` : ''}`;
  const prompt = `상품명: ${p.originalName}
카테고리: ${p.categoryPath || '미상'}
핵심 특징: ${(p.features || []).join(', ') || '미상'}
강조점: ${persona.focus}

[문체 참고 — 이 상품 종류(${kind})의 실제 후기는 이런 결이다(내용·문장 절대 베끼지 말 것, 결만 참고)]
${guide.shot}

⚠️ 위 예시는 "${leaf}"의 감각(${guide.sensory})에 맞춰 새로 쓴다. 예시가 다른 상품이면
   그 상품의 감각 표현을 가져오지 말 것(먹는 것이 아니면 맛·식감·과즙 금지).

위 "${leaf}" 상품의 쿠팡 상세페이지 본문을, 블로그 후기 글처럼(장면 → 감각 → 그래서 뭐가
좋았는지) 써줘. 라벨식 카탈로그 문장과 상투구는 절대 쓰지 말고, SEO 분량·키워드는 맞춰서.`;
  // temperature 0.85 — 후기체는 상투구 회피가 관건이라 다양성을 조금 더 준다(검증 루프가 잡는다).
  return { system, prompt, options: { temperature: 0.85, num_predict: maxTokens } };
}
