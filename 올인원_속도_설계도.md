# 올인원 100개를 20분 안에 — 속도 설계도

> 목표: **상품 100개 · 20분(1,200초) = 상품당 12초.** 기능·품질은 지금 그대로.
> 실측 기준 PC: RTX 4060 Ti 16GB (여유 VRAM 13.7GB), ollama, exaone3.5:7.8b + qwen2.5vl:7b.
> 측정일: 2026-08-20. 이 문서의 모든 숫자는 그 PC에서 직접 잰 값이다(추정치는 "추정"이라 적었다).

---

## 1. 지금 어디에 시간이 가는가 (실측)

### 1-1. 텍스트 생성 — GPU는 이미 포화 상태다

동일 프롬프트로 동시 요청 수만 바꿔가며 잰 총 처리량:

| 동시 | 총시간 | 합계 tok/s | 배수 |
|---:|---:|---:|---:|
| 1 | 6.4s | 46.7 | 1.00x |
| 2 | 6.8s | 88.0 | 1.88x |
| 4 | 7.6s | 158.3 | 3.39x |
| **6** | **9.3s** | **193.8** | **4.15x** |
| 8 | 15.9s | 151.0 | 3.24x ← 슬롯 초과로 줄서기 |

**결론: 이 GPU의 텍스트 상한은 ~194 tok/s이고, 동시 6에서 이미 도달해 있다.**
`pickGenProfile()` 이 여유 VRAM 11GB↑에서 concurrency 6을 주고 있으므로 **이 PC는 이미 최적**이다.
텍스트 단계를 더 빠르게 만들 여지는 "동시성"에는 없다 — 남은 건 **생성 토큰 수**뿐이다.

상품당 출력 토큰: 상세 800 + (노출명·카테고리·옵션) ≈ 360 → **약 1,160 토큰**
→ 194 tok/s ÷ 1,160 = **상품당 6.0초**, 100개 = **약 600초(10분)**

> 참고로 `OLLAMA_FLASH_ATTENTION=1` + `OLLAMA_KV_CACHE_TYPE=q8_0` 로 별도 서버를 띄워 비교했다.
> 최대 처리량은 194 → 193으로 **변화 없음**(이미 대역폭 포화). 다만 동시 8에서 무너지지 않았다
> (151 → 193). 즉 이 옵션은 **속도가 아니라 슬롯 안정성·KV 메모리 절감** 용도다 —
> 8GB급 GPU에서 슬롯 수를 늘리는 데 의미가 있다.

### 1-2. 비전(대표컷 판정) — 여기가 진짜 병목이다

한 호출을 구간별로 쪼개 보면:

```
1회(콜드): 총 26,623ms | 모델적재 18,344ms | prefill 895ms | decode 181ms
2회      : 총  9,891ms | 모델적재    835ms | prefill  22ms | decode 183ms
3회      : 총 10,625ms | 모델적재    628ms | prefill  23ms | decode 180ms
```

**추론 자체는 0.2초다. 그런데 한 호출이 10초 걸린다.** ollama가 보고하는 구간(적재·prefill·decode)의
합이 1초인데 벽시계는 10초 — 차이 **약 9초는 이미지 인코딩(비전 타워)** 이다. 이건 출력 토큰이
아니라 **입력 이미지 픽셀 수에 비례**한다.

현재 입력은 `buildContactSheet` 가 만드는 **24칸 격자 · 셀 300px · 4열 = 1,200 × 1,800px** 이다.
상품당 1콜이라도 **8~15초**(실측 5~9초는 그보다 작은 단일 이미지 기준, 격자는 더 큼).

→ 100개 = **800~1,500초 (13~25분).** 텍스트보다 크다.

### 1-3. 누끼(ComfyUI/SDXL) 및 오버헤드

- 대표컷 누끼: 상품당 2~5초(gpu-lease 적용 후) → 100개 **200~500초**
- ComfyUI 정지→재기동 스왑, 모델 콜드 적재(18초), 원본명 조회(최대 120초)

### 1-4. 합계

| 단계 | 100개 소요 | 비고 |
|---|---:|---|
| 비전 | 800~1,500s | 순차, GPU 독점 |
| 텍스트 | 600s | 동시 6, 이미 포화 |
| 누끼 | 200~500s | 순차, GPU 독점 |
| 오버헤드 | 100~200s | 스왑·콜드적재·원본명 |
| **합계** | **1,700~2,800s (28~47분)** | |

목표는 1,200초. **최소 500초, 많게는 1,600초를 덜어내야 한다.**

---

## 2. 설계 — 무엇을 바꾸는가

세 단계가 **각자 GPU를 독점하고 순서대로** 돈다는 게 구조의 핵심이다. 총시간 = 단계의 합.
따라서 방법은 셋뿐이다: ① 일을 줄인다 ② 겹친다 ③ 나중으로 미룬다.

### P1. 비전 격자를 CLIP이 고른 후보로만 채운다 — **-600~1,200초** ⭐최대

**지금**: 상품의 사진 24장을 전부 격자에 넣고 VLM에게 "각 칸이 무엇인지" 묻는다.
**바꿈**: CLIP(CPU, 이미 있음)으로 먼저 점수를 매겨 **상위 4~6장만** 격자에 넣는다.

- 이미지 토큰이 24칸 → 5칸으로 줄어 **인코딩 시간이 약 1/5**(9초 → 2초 내외, 추정)
- 판정 품질: VLM이 하는 일은 "이 칸이 정면 단독 상품컷인가 / 로고·배송배너·리뷰캡처인가"다.
  CLIP이 명백한 하위 후보(로고·텍스처)를 떨어뜨려도 **VLM이 고를 후보군은 그대로 남는다.**
- ⚠️ 리스크: CLIP이 상위 4장 안에 정답을 못 넣으면 VLM도 못 고른다.
  → **완화**: 상위 N을 6으로 잡고, CLIP 점수 1~2위 격차가 작으면(불확실) N을 12로 늘린다.

### P2. 누끼를 "등록 직전"으로 미룬다 — **-200~500초**

검수 화면에서 사람이 보는 건 대표컷 선택과 글이다. **누끼된 이미지는 쿠팡 등록 시점에만 필요**하다.
생성 파이프라인에서 빼고, 검수 통과·등록 시 백그라운드로 돌린다.

- 기능 손실 없음(같은 이미지를 같은 코드로 만든다 — 시점만 옮긴다)
- 부수 효과: 텍스트 단계 동안 ComfyUI를 내렸다 올리는 **VRAM 스왑 자체가 사라진다**
- ⚠️ 등록 버튼을 누른 뒤 누끼를 기다리게 되면 안 된다 → 검수 화면 진입 시점에 백그라운드 시작

### P3. 비전과 텍스트를 겹친다 (VRAM이 되는 PC만) — **-최대 600초**

지금은 "VLM은 GPU라 텍스트와 동시 실행 불가"라는 전제로 비전을 먼저 끝내고 언로드한다.
**실측은 그 전제와 다르다** — 두 모델을 동시에 올려두고 비전을 돌려도 느려지지 않았다:

```
A) 비전만 상주      : 4장 34.2s · 장당 8.6s
B) 텍스트도 함께 상주: 4장 19.9s · 장당 5.0s   ← 오히려 빨랐다
```

두 모델 합 10.8GB < 여유 13.7GB이므로 경합이 아니라 **공존**이 된다.
→ 겹치면 비전 시간이 텍스트 뒤로 **통째로 숨는다.**

- 게이트: `freeVram ≥ (텍스트모델 + 비전모델 + KV여유)` 일 때만 겹친다. 아니면 지금처럼 순차.
- ⚠️ 8GB GPU는 겹칠 수 없다 → 그 PC는 P1·P2로만 간다(아래 4장 참조)

### P4. 상세 재생성을 줄인다 — **-50~200초**

`generatePerfectDetail` 은 검증 실패 시 최대 2회 생성한다. 재생성 1회 = 상세 1개 값(800토큰 ≈ 4초).
첫 통과율이 곧 시간이다. 실패 사유를 집계해(길이/후킹/반복/SEO 중 무엇인지) 상위 사유를
프롬프트에서 선제 차단한다. **검증 기준은 낮추지 않는다** — 낮추면 품질이 내려간다.

### P5. 콜드 적재를 1회로 고정 — **-30~60초**

- 비전 모델 첫 적재 18초는 피할 수 없다. 단 **단계 사이에 언로드/재적재가 반복되면** 그만큼 곱해진다.
- `keep_alive` 를 파이프라인 전 구간보다 길게 잡고, P2로 ComfyUI 스왑이 사라지면 재적재 사유가 없다.

---

## 3. 예산표 — 이렇게 하면 20분에 들어오는가

**16GB GPU (겹치기 가능)**

| 단계 | 지금 | 설계 후 | 근거 |
|---|---:|---:|---|
| 비전 | 800~1,500s | **0s** | P1로 200~300s로 줄이고, P3로 텍스트 뒤에 숨김 |
| 텍스트 | 600s | 600s | 이미 포화(194 tok/s) — 손대지 않음 |
| 누끼 | 200~500s | **0s** | P2로 등록 시점 이동 |
| 오버헤드 | 100~200s | 60s | P5 |
| **합계** | **28~47분** | **≈ 11분** | 목표 20분 대비 **9분 여유** |

**8GB GPU (겹치기 불가, 텍스트 동시 2~3)**

| 단계 | 설계 후 | 근거 |
|---|---:|---|
| 비전 | 250s | P1 (격자 1/5) |
| 텍스트 | 900~1,200s | 동시 2~3 → 100~150 tok/s 추정 |
| 누끼 | 0s | P2 |
| **합계** | **≈ 20~24분** | 경계선 — P4까지 해야 들어온다 |

**GPU 없는 PC: 100개 20분은 불가능하다.**
CPU 추론은 3B 모델로도 상세 1개에 수십 초다(실측 참고: 무GPU 비전 1콜 88초).
이 구간은 코드로 못 이긴다 — **서버(클라우드 GPU) 오프로드가 유일한 답**이며 별도 설계가 필요하다.
지금처럼 "느리지만 된다"로 두되, 시작 전에 예상 시간을 정직하게 보여주는 현재 동작(`confirmSlow`)을 유지한다.

---

## 4. 구현 순서 (효과/위험 비)

| 순서 | 항목 | 예상 절감 | 위험 | 범위 |
|---|---|---:|---|---|
| 1 | **P2 누끼 지연** | 200~500s | 낮음 | 워커 + 웹(등록 시점 훅) |
| 2 | **P1 격자 축소** | 600~1,200s | 중간(대표컷 정확도) | 워커 `vision-selector.mjs` |
| 3 | **P3 겹치기** | 최대 600s | 중간(저VRAM PC 회귀) | 워커 `run-folder.mjs` 게이트 |
| 4 | P5 적재 1회 | 30~60s | 낮음 | 워커 |
| 5 | P4 재생성 감소 | 50~200s | 낮음 | `detail-content-gen.mjs` |

P1과 P3은 **둘 중 하나만 해도 목표에 들어간다.** 위험이 낮은 P2 → P3 → P1 순서를 권한다
(P1은 대표컷 품질에 직접 닿으므로 마지막에, 반드시 A/B로 검증).

---

## 5. 검증 — "빨라졌는데 나빠졌다"를 막는 법  ✅ 구현됨 (2026-09-01)

속도 개선은 품질 저하를 숨기기 쉽다. 이 장의 두 가지가 **코드에 들어갔다.**

1. **A/B 회귀 하네스** — `worker/bench-allinone.mjs`

   ```bash
   node worker/bench-allinone.mjs --baseline "D:소싱표본20"     # 지금 설정으로 기준선 저장
   MEGALOAD_VISION_CELLS=12 node worker/bench-allinone.mjs --compare "D:소싱표본20"
   ```

   대표컷 동일 비율 · 리뷰컷/상세컷 집합일치(Jaccard) · 검수필요 증감 · 단계별 시간을 한 화면에
   찍고, **대표컷 일치 95% 미만이거나 검수필요가 늘면 종료코드 1**로 떨어진다(CI 에 그대로 쓸 수 있다).
   run-folder 를 직접 실행하므로 "측정 대상과 실제 동작이 다른" 일이 없다.

2. **단계별 실측이 파일로 남는다** — `_allinone.timing.json`

   `run-folder` 가 끝날 때 저장한다: 상품당/100개 환산 · 단계(recog·text·thumb·other) ·
   비전 구간(격자 CPU vs 모델 GPU, 콜 수·칸 수·재질문·상한초과) · 그때의 설정(동시성·셀·칸·겹치기).
   도우미 manifest 가 웹에 같이 실어 주고, **검수 화면의 "이번 작업" 리포트가 실측을 그대로 보여준다**
   — 목표(상품당 12초) 대비 어디가 컸는지가 사람 눈에 바로 보인다.

   ```
   실측: 상품당 8.4초 · 100개 환산 14.0분 | 인식 3.1초(격자 0.6 + 모델 2.5, 2콜/18칸) · 텍스트 5.2초 · 누끼 0.0초
   ```

측정 하네스는 이 밖에 `worker/bench-aigen.mjs`(텍스트 처리량)가 있다.

---

## 6. 하지 말아야 할 것 (실측 근거)

- **상세 토큰(800)을 낮추기**: `num_predict` 는 상한이라 낮춰도 빨라지지 않고 글만 잘린다.
  잘린 글은 길이 검증에 걸려 재생성을 불러 **오히려 느려진다**(기존 실측: 상한 400 → 재생성 4/4).
- **텍스트 동시성을 8 이상으로**: 슬롯 초과로 줄서기 → 194 → 151 tok/s 로 **떨어진다**.
- **flash-attention/KV 양자화를 속도 대책으로 기대하기**: 이 GPU에선 최대 처리량 변화 없음(194 → 193).
  8GB급에서 **슬롯을 늘리는 수단**으로만 의미가 있다.
